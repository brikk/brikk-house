# Runtime surface for brikk-sql virtual pipelines: the types user code references
# (Shape/Partial/Rel, @BrikkSql/@BrikkTrait, Sql.<dialect>()) and the rendering of a
# composed pipeline value back to dialect SQL via brikk-sql.
product: jvm/lib

dependencies:
  - ../brikk-sql: exported

settings:
  kotlin:
    version: 2.4.0
