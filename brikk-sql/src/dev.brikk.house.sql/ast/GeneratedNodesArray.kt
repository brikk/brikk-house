// GENERATED FILE — DO NOT EDIT.
// Generated by tools/gen_ast_nodes.py from sqlglot v30.12.0-44-g93d16591.
// sqlglot is MIT-licensed, copyright Toby Mao. See reference/sqlglot/LICENSE.
package dev.brikk.house.sql.ast

// sqlglot.expressions.array: 66 generated classes.

// sqlglot: array.Apply(Expression, Func)
class Apply(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: array.Array(Expression, Func) — is_var_len_args
class Array(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false, "bracket_notation" to false,
            "struct_name_inheritance" to false,
        )
    }
}

// sqlglot: array.ArrayAll(Expression, Func)
class ArrayAll(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: array.ArrayAny(Expression, Func)
class ArrayAny(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: array.ArrayAppend(Expression, Func)
class ArrayAppend(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "null_propagation" to false,
        )
    }
}

// sqlglot: array.ArrayCompact(Expression, Func)
class ArrayCompact(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.ArrayConcat(Expression, Func) — _sql_names = ['ARRAY_CONCAT', 'ARRAY_CAT'], is_var_len_args
class ArrayConcat(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to false, "null_propagation" to false,
        )
        private val SQL_NAMES = listOf("ARRAY_CONCAT", "ARRAY_CAT")
    }
}

// sqlglot: array.ArrayConstructCompact(Expression, Func) — is_var_len_args
class ArrayConstructCompact(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false,
        )
    }
}

// sqlglot: array.ArrayDistinct(Expression, Func)
class ArrayDistinct(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "check_null" to false,
        )
    }
}

// sqlglot: array.ArrayExcept(Expression, Func)
class ArrayExcept(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "is_multiset" to false,
        )
    }
}

// sqlglot: array.ArrayFilter(Expression, Func) — _sql_names = ['FILTER', 'ARRAY_FILTER']
class ArrayFilter(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
        private val SQL_NAMES = listOf("FILTER", "ARRAY_FILTER")
    }
}

// sqlglot: array.ArrayFirst(Expression, Func)
class ArrayFirst(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: array.ArrayInsert(Expression, Func)
class ArrayInsert(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "position" to true, "expression" to true, "offset" to false,
        )
    }
}

// sqlglot: array.ArrayIntersect(Expression, Func) — _sql_names = ['ARRAY_INTERSECT', 'ARRAY_INTERSECTION'], is_var_len_args
class ArrayIntersect(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true, "is_multiset" to false,
        )
        private val SQL_NAMES = listOf("ARRAY_INTERSECT", "ARRAY_INTERSECTION")
    }
}

// sqlglot: array.ArrayLast(Expression, Func)
class ArrayLast(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.ArrayMax(Expression, Func)
class ArrayMax(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.ArrayMin(Expression, Func)
class ArrayMin(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.ArrayPrepend(Expression, Func)
class ArrayPrepend(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "null_propagation" to false,
        )
    }
}

// sqlglot: array.ArrayRemove(Expression, Func)
class ArrayRemove(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "null_propagation" to false,
        )
    }
}

// sqlglot: array.ArrayRemoveAt(Expression, Func)
class ArrayRemoveAt(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "position" to true,
        )
    }
}

// sqlglot: array.ArrayReverse(Expression, Func)
class ArrayReverse(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.ArraySize(Expression, Func) — _sql_names = ['ARRAY_SIZE', 'ARRAY_LENGTH']
class ArraySize(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
        private val SQL_NAMES = listOf("ARRAY_SIZE", "ARRAY_LENGTH")
    }
}

// sqlglot: array.ArraySlice(Expression, Func)
class ArraySlice(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "start" to true, "end" to false, "step" to false,
            "zero_based" to false,
        )
    }
}

// sqlglot: array.ArraySort(Expression, Func)
class ArraySort(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: array.ArraySum(Expression, Func)
class ArraySum(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: array.ArrayToString(Expression, Func) — _sql_names = ['ARRAY_TO_STRING', 'ARRAY_JOIN']
class ArrayToString(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "null" to false, "null_is_empty" to false,
            "null_delim_is_null" to false,
        )
        private val SQL_NAMES = listOf("ARRAY_TO_STRING", "ARRAY_JOIN")
    }
}

// sqlglot: array.ArraysZip(Expression, Func) — is_var_len_args
class ArraysZip(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false,
        )
    }
}

// sqlglot: array.Explode(Expression, Func, UDTF) — is_var_len_args
open class Explode(initArgs: Args = emptyMap()) : Expression(initArgs), Func, UDTF {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to false,
        )
    }
}

// sqlglot: array.Flatten(Expression, Func)
class Flatten(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.GenerateSeries(Expression, Func)
open class GenerateSeries(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "start" to true, "end" to true, "step" to false, "is_end_exclusive" to false,
        )
    }
}

// sqlglot: array.Generator(Expression, Func, UDTF)
class Generator(initArgs: Args = emptyMap()) : Expression(initArgs), Func, UDTF {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "rowcount" to false, "timelimit" to false,
        )
    }
}

// sqlglot: array.Inline(Expression, Func)
class Inline(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.List(Expression, Func) — is_var_len_args
class List(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false,
        )
    }
}

// sqlglot: array.Map(Expression, Func)
class Map(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "keys" to false, "values" to false,
        )
    }
}

// sqlglot: array.MapCat(Expression, Func)
class MapCat(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: array.MapContainsKey(Expression, Func)
class MapContainsKey(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "key" to true,
        )
    }
}

// sqlglot: array.MapDelete(Expression, Func) — is_var_len_args
class MapDelete(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to true,
        )
    }
}

// sqlglot: array.MapFromEntries(Expression, Func)
class MapFromEntries(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.MapInsert(Expression, Func)
class MapInsert(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "key" to false, "value" to true, "update_flag" to false,
        )
    }
}

// sqlglot: array.MapKeys(Expression, Func)
class MapKeys(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.MapPick(Expression, Func) — is_var_len_args
class MapPick(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to true,
        )
    }
}

// sqlglot: array.MapSize(Expression, Func)
class MapSize(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.PositionalColumn(Expression)
class PositionalColumn(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: array.Reduce(Expression, Func)
class Reduce(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "initial" to true, "merge" to true, "finish" to false,
        )
    }
}

// sqlglot: array.SortArray(Expression, Func)
class SortArray(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "asc" to false, "nulls_first" to false,
        )
    }
}

// sqlglot: array.StDistance(Expression, Func)
class StDistance(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "use_spheroid" to false,
        )
    }
}

// sqlglot: array.StPoint(Expression, Func) — _sql_names = ['ST_POINT', 'ST_MAKEPOINT']
class StPoint(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "z" to false, "m" to false,
        )
        private val SQL_NAMES = listOf("ST_POINT", "ST_MAKEPOINT")
    }
}

// sqlglot: array.StarMap(Expression, Func)
class StarMap(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.StringToArray(Expression, Func) — _sql_names = ['STRING_TO_ARRAY', 'SPLIT_BY_STRING']
class StringToArray(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false, "null" to false,
        )
        private val SQL_NAMES = listOf("STRING_TO_ARRAY", "SPLIT_BY_STRING")
    }
}

// sqlglot: array.StrtokToArray(Expression, Func)
class StrtokToArray(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: array.Struct(Expression, Func) — is_var_len_args
class Struct(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false,
        )
    }
}

// sqlglot: array.StructExtract(Expression, Func)
class StructExtract(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: array.ToArray(Expression, Func)
class ToArray(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.ToMap(Expression, Func)
class ToMap(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: array.Transform(Expression, Func)
class Transform(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: array.Unnest(Expression, Func, UDTF)
class Unnest(initArgs: Args = emptyMap()) : Expression(initArgs), Func, UDTF {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true, "alias" to false, "offset" to false, "explode_array" to false,
        )
    }
}

// sqlglot: array.VarMap(Expression, Func) — is_var_len_args
class VarMap(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "keys" to true, "values" to true,
        )
    }
}

// sqlglot: array.ArrayContainedBy(Expression, Binary, Func)
class ArrayContainedBy(initArgs: Args = emptyMap()) : Binary(initArgs), Func

// sqlglot: array.ArrayContains(Expression, Binary, Func) — _sql_names = ['ARRAY_CONTAINS', 'ARRAY_HAS']
class ArrayContains(initArgs: Args = emptyMap()) : Binary(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "ensure_variant" to false, "check_null" to false,
        )
        private val SQL_NAMES = listOf("ARRAY_CONTAINS", "ARRAY_HAS")
    }
}

// sqlglot: array.ArrayContainsAll(Expression, Binary, Func) — _sql_names = ['ARRAY_CONTAINS_ALL', 'ARRAY_HAS_ALL']
class ArrayContainsAll(initArgs: Args = emptyMap()) : Binary(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("ARRAY_CONTAINS_ALL", "ARRAY_HAS_ALL")
    }
}

// sqlglot: array.ArrayOverlaps(Expression, Binary, Func)
class ArrayOverlaps(initArgs: Args = emptyMap()) : Binary(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "null_safe" to false,
        )
    }
}

// sqlglot: array.ArrayPosition(Expression, Binary, Func)
class ArrayPosition(initArgs: Args = emptyMap()) : Binary(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "zero_based" to false,
        )
    }
}

// sqlglot: array.ExplodingGenerateSeries(GenerateSeries)
class ExplodingGenerateSeries(initArgs: Args = emptyMap()) : GenerateSeries(initArgs)

// sqlglot: array.Posexplode(Explode)
open class Posexplode(initArgs: Args = emptyMap()) : Explode(initArgs)

// sqlglot: array._ExplodeOuter(Explode, ExplodeOuter) — _sql_names = ['EXPLODE_OUTER']
class _ExplodeOuter(initArgs: Args = emptyMap()) : Explode(initArgs), ExplodeOuter {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("EXPLODE_OUTER")
    }
}

// sqlglot: array.PosexplodeOuter(Posexplode, ExplodeOuter)
class PosexplodeOuter(initArgs: Args = emptyMap()) : Posexplode(initArgs), ExplodeOuter
