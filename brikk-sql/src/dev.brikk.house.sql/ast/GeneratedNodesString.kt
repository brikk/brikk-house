// GENERATED FILE — DO NOT EDIT.
// Generated by tools/gen_ast_nodes.py from sqlglot v30.12.0-44-g93d16591.
// sqlglot is MIT-licensed, copyright Toby Mao. See reference/sqlglot/LICENSE.
package dev.brikk.house.sql.ast

// sqlglot.expressions.string: 105 generated classes.

// sqlglot: string.Ascii(Expression, Func)
class Ascii(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.Base64DecodeBinary(Expression, Func)
class Base64DecodeBinary(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "alphabet" to false,
        )
    }
}

// sqlglot: string.Base64DecodeString(Expression, Func)
class Base64DecodeString(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "alphabet" to false,
        )
    }
}

// sqlglot: string.Base64Encode(Expression, Func)
class Base64Encode(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "max_line_length" to false, "alphabet" to false,
        )
    }
}

// sqlglot: string.BitLength(Expression, Func)
class BitLength(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.ByteLength(Expression, Func)
class ByteLength(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.Chr(Expression, Func) — _sql_names = ['CHR', 'CHAR'], is_var_len_args
class Chr(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true, "charset" to false,
        )
        private val SQL_NAMES = listOf("CHR", "CHAR")
    }
}

// sqlglot: string.CityHash64(Expression, Func) — is_var_len_args
class CityHash64(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false,
        )
    }
}

// sqlglot: string.CodePointsToBytes(Expression, Func)
class CodePointsToBytes(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.CodePointsToString(Expression, Func)
class CodePointsToString(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.Compress(Expression, Func)
class Compress(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "method" to false,
        )
    }
}

// sqlglot: string.Concat(Expression, Func) — is_var_len_args
open class Concat(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true, "safe" to false, "coalesce" to false,
        )
    }
}

// sqlglot: string.Contains(Expression, Func)
class Contains(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "json_scope" to false,
        )
    }
}

// sqlglot: string.ConvertToCharset(Expression, Func)
class ConvertToCharset(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "dest" to true, "source" to false,
        )
    }
}

// sqlglot: string.Decode(Expression, Func)
class Decode(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "charset" to true, "replace" to false,
        )
    }
}

// sqlglot: string.DecompressBinary(Expression, Func)
class DecompressBinary(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "method" to true,
        )
    }
}

// sqlglot: string.DecompressString(Expression, Func)
class DecompressString(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "method" to true,
        )
    }
}

// sqlglot: string.Decrypt(Expression, Func)
class Decrypt(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "passphrase" to true, "aad" to false, "encryption_method" to false,
            "safe" to false,
        )
    }
}

// sqlglot: string.DecryptRaw(Expression, Func)
class DecryptRaw(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "key" to true, "iv" to true, "aad" to false,
            "encryption_method" to false, "aead" to false, "safe" to false,
        )
    }
}

// sqlglot: string.Elt(Expression, Func) — is_var_len_args
class Elt(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to true,
        )
    }
}

// sqlglot: string.Encode(Expression, Func)
class Encode(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "charset" to true,
        )
    }
}

// sqlglot: string.Encrypt(Expression, Func)
class Encrypt(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "passphrase" to true, "aad" to false, "encryption_method" to false,
        )
    }
}

// sqlglot: string.EncryptRaw(Expression, Func)
class EncryptRaw(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "key" to true, "iv" to true, "aad" to false,
            "encryption_method" to false,
        )
    }
}

// sqlglot: string.EndsWith(Expression, Func) — _sql_names = ['ENDS_WITH', 'ENDSWITH']
class EndsWith(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
        private val SQL_NAMES = listOf("ENDS_WITH", "ENDSWITH")
    }
}

// sqlglot: string.FarmFingerprint(Expression, Func) — _sql_names = ['FARM_FINGERPRINT', 'FARMFINGERPRINT64'], is_var_len_args
class FarmFingerprint(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true,
        )
        private val SQL_NAMES = listOf("FARM_FINGERPRINT", "FARMFINGERPRINT64")
    }
}

// sqlglot: string.Format(Expression, Func) — is_var_len_args
class Format(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to false,
        )
    }
}

// sqlglot: string.FromBase(Expression, Func)
class FromBase(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: string.FromBase32(Expression, Func)
class FromBase32(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.FromBase64(Expression, Func)
class FromBase64(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.Hex(Expression, Func)
open class Hex(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "case" to false,
        )
    }
}

// sqlglot: string.HexDecodeString(Expression, Func)
class HexDecodeString(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.Initcap(Expression, Func)
class Initcap(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: string.IsAscii(Expression, Func)
class IsAscii(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.Left(Expression, Func)
class Left(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "negative_length_returns_empty" to false,
        )
    }
}

// sqlglot: string.Length(Expression, Func) — _sql_names = ['LENGTH', 'LEN', 'CHAR_LENGTH', 'CHARACTER_LENGTH']
class Length(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "binary" to false, "encoding" to false,
        )
        private val SQL_NAMES = listOf("LENGTH", "LEN", "CHAR_LENGTH", "CHARACTER_LENGTH")
    }
}

// sqlglot: string.Levenshtein(Expression, Func)
class Levenshtein(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false, "ins_cost" to false, "del_cost" to false,
            "sub_cost" to false, "max_dist" to false,
        )
    }
}

// sqlglot: string.Lower(Expression, Func) — _sql_names = ['LOWER', 'LCASE']
class Lower(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("LOWER", "LCASE")
    }
}

// sqlglot: string.MD5(Expression, Func) — _sql_names = ['MD5']
class MD5(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("MD5")
    }
}

// sqlglot: string.MD5Digest(Expression, Func) — _sql_names = ['MD5_DIGEST'], is_var_len_args
class MD5Digest(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to false,
        )
        private val SQL_NAMES = listOf("MD5_DIGEST")
    }
}

// sqlglot: string.MD5NumberLower64(Expression, Func)
class MD5NumberLower64(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.MD5NumberUpper64(Expression, Func)
class MD5NumberUpper64(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.MatchAgainst(Expression, Func)
class MatchAgainst(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to true, "modifier" to false,
        )
    }
}

// sqlglot: string.Normalize(Expression, Func)
class Normalize(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "form" to false, "is_casefold" to false,
        )
    }
}

// sqlglot: string.NumberToStr(Expression, Func)
class NumberToStr(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to true, "culture" to false,
        )
    }
}

// sqlglot: string.Overlay(Expression, Func)
class Overlay(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "from_" to true, "for_" to false,
        )
    }
}

// sqlglot: string.Pad(Expression, Func)
class Pad(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "fill_pattern" to false, "is_left" to true,
        )
    }
}

// sqlglot: string.ParseBignumeric(Expression, Func)
class ParseBignumeric(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.ParseNumeric(Expression, Func)
class ParseNumeric(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.ParseUrl(Expression, Func)
class ParseUrl(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "part_to_extract" to false, "key" to false, "permissive" to false,
        )
    }
}

// sqlglot: string.RegexpCount(Expression, Func)
class RegexpCount(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "position" to false, "parameters" to false,
        )
    }
}

// sqlglot: string.RegexpExtract(Expression, Func)
class RegexpExtract(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "position" to false, "occurrence" to false,
            "parameters" to false, "group" to false, "null_if_pos_overflow" to false,
        )
    }
}

// sqlglot: string.RegexpExtractAll(Expression, Func)
class RegexpExtractAll(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "group" to false, "parameters" to false,
            "position" to false, "occurrence" to false,
        )
    }
}

// sqlglot: string.RegexpInstr(Expression, Func)
class RegexpInstr(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "position" to false, "occurrence" to false,
            "option" to false, "parameters" to false, "group" to false,
        )
    }
}

// sqlglot: string.RegexpReplace(Expression, Func)
class RegexpReplace(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "replacement" to false, "position" to false,
            "occurrence" to false, "modifiers" to false, "single_replace" to false,
        )
    }
}

// sqlglot: string.RegexpSplit(Expression, Func)
class RegexpSplit(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "limit" to false,
        )
    }
}

// sqlglot: string.RegexpSubstr(Expression, Func)
class RegexpSubstr(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "position" to false, "occurrence" to false,
            "parameters" to false, "group" to false,
        )
    }
}

// sqlglot: string.Repeat(Expression, Func)
class Repeat(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "times" to true,
        )
    }
}

// sqlglot: string.Replace(Expression, Func)
class Replace(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "replacement" to false,
        )
    }
}

// sqlglot: string.Reverse(Expression, Func)
class Reverse(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.Right(Expression, Func)
class Right(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "negative_length_returns_empty" to false,
        )
    }
}

// sqlglot: string.RtrimmedLength(Expression, Func)
class RtrimmedLength(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.SHA(Expression, Func) — _sql_names = ['SHA', 'SHA1']
class SHA(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("SHA", "SHA1")
    }
}

// sqlglot: string.SHA1Digest(Expression, Func)
class SHA1Digest(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.SHA2(Expression, Func) — _sql_names = ['SHA2']
class SHA2(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "length" to false,
        )
        private val SQL_NAMES = listOf("SHA2")
    }
}

// sqlglot: string.SHA2Digest(Expression, Func)
class SHA2Digest(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "length" to false,
        )
    }
}

// sqlglot: string.SafeConvertBytesToString(Expression, Func)
class SafeConvertBytesToString(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.Search(Expression, Func)
class Search(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "json_scope" to false, "analyzer" to false,
            "analyzer_options" to false, "search_mode" to false,
        )
    }
}

// sqlglot: string.SearchIp(Expression, Func)
class SearchIp(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: string.Soundex(Expression, Func)
class Soundex(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.SoundexP123(Expression, Func)
class SoundexP123(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.Space(Expression, Func)
class Space(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.Split(Expression, Func)
class Split(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "limit" to false, "null_returns_null" to false,
            "empty_delimiter_returns_whole" to false,
        )
    }
}

// sqlglot: string.SplitPart(Expression, Func)
class SplitPart(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "delimiter" to false, "part_index" to false,
            "part_index_zero_as_one" to false, "empty_delimiter_returns_whole" to false,
        )
    }
}

// sqlglot: string.StandardHash(Expression, Func)
class StandardHash(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: string.StartsWith(Expression, Func) — _sql_names = ['STARTS_WITH', 'STARTSWITH']
class StartsWith(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
        private val SQL_NAMES = listOf("STARTS_WITH", "STARTSWITH")
    }
}

// sqlglot: string.StrPosition(Expression, Func)
class StrPosition(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "substr" to true, "position" to false, "occurrence" to false,
            "clamp_position" to false,
        )
    }
}

// sqlglot: string.StrToMap(Expression, Func)
class StrToMap(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "pair_delim" to false, "key_value_delim" to false,
            "duplicate_resolution_callback" to false,
        )
    }
}

// sqlglot: string.String(Expression, Func)
class String(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "zone" to false,
        )
    }
}

// sqlglot: string.Strtok(Expression, Func)
class Strtok(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "delimiter" to false, "part_index" to false,
        )
    }
}

// sqlglot: string.Stuff(Expression, Func) — _sql_names = ['STUFF', 'INSERT']
class Stuff(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "start" to true, "length" to true, "expression" to true,
        )
        private val SQL_NAMES = listOf("STUFF", "INSERT")
    }
}

// sqlglot: string.Substring(Expression, Func) — _sql_names = ['SUBSTRING', 'SUBSTR']
class Substring(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "start" to false, "length" to false, "zero_start" to false,
        )
        private val SQL_NAMES = listOf("SUBSTRING", "SUBSTR")
    }
}

// sqlglot: string.SubstringIndex(Expression, Func)
class SubstringIndex(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "delimiter" to true, "count" to true,
        )
    }
}

// sqlglot: string.ToBase32(Expression, Func)
class ToBase32(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.ToBase64(Expression, Func)
class ToBase64(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.ToBinary(Expression, Func)
class ToBinary(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to false, "safe" to false,
        )
    }
}

// sqlglot: string.ToChar(Expression, Func)
class ToChar(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to false, "nlsparam" to false, "is_numeric" to false,
        )
    }
}

// sqlglot: string.ToCodePoints(Expression, Func)
class ToCodePoints(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.ToDecfloat(Expression, Func)
class ToDecfloat(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to false,
        )
    }
}

// sqlglot: string.ToDouble(Expression, Func)
class ToDouble(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to false, "safe" to false,
        )
    }
}

// sqlglot: string.ToFile(Expression, Func)
class ToFile(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "path" to false, "safe" to false,
        )
    }
}

// sqlglot: string.ToNumber(Expression, Func)
class ToNumber(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to false, "nlsparam" to false, "precision" to false,
            "scale" to false, "safe" to false, "safe_name" to false, "default" to false,
        )
    }
}

// sqlglot: string.Translate(Expression, Func)
class Translate(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "from_" to true, "to" to true,
        )
    }
}

// sqlglot: string.Trim(Expression, Func)
class Trim(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false, "position" to false, "collation" to false,
        )
    }
}

// sqlglot: string.TryBase64DecodeBinary(Expression, Func)
class TryBase64DecodeBinary(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "alphabet" to false,
        )
    }
}

// sqlglot: string.TryBase64DecodeString(Expression, Func)
class TryBase64DecodeString(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "alphabet" to false,
        )
    }
}

// sqlglot: string.TryHexDecodeBinary(Expression, Func)
class TryHexDecodeBinary(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.TryHexDecodeString(Expression, Func)
class TryHexDecodeString(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: string.TryToDecfloat(Expression, Func)
class TryToDecfloat(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to false,
        )
    }
}

// sqlglot: string.Unhex(Expression, Func)
class Unhex(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: string.Unicode(Expression, Func)
class Unicode(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "empty_is_zero" to false,
        )
    }
}

// sqlglot: string.Upper(Expression, Func) — _sql_names = ['UPPER', 'UCASE']
class Upper(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("UPPER", "UCASE")
    }
}

// sqlglot: string.ConcatWs(Concat)
class ConcatWs(initArgs: Args = emptyMap()) : Concat(initArgs)

// sqlglot: string.LowerHex(Hex)
class LowerHex(initArgs: Args = emptyMap()) : Hex(initArgs)

// sqlglot: string.RegexpFullMatch(Expression, Binary, Func)
class RegexpFullMatch(initArgs: Args = emptyMap()) : Binary(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "options" to false,
        )
    }
}

// sqlglot: string.RegexpILike(Expression, Binary, Func)
class RegexpILike(initArgs: Args = emptyMap()) : Binary(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "flag" to false,
        )
    }
}
