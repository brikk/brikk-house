// GENERATED FILE — DO NOT EDIT.
// Generated by tools/gen_ast_nodes.py from sqlglot v30.12.0-44-g93d16591.
// sqlglot is MIT-licensed, copyright Toby Mao. See reference/sqlglot/LICENSE.
package dev.brikk.house.sql.ast

// sqlglot.expressions.json: 35 generated classes.

// sqlglot: json.CheckJson(Expression, Func)
class CheckJson(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: json.JSONArray(Expression, Func) — is_var_len_args
class JSONArray(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false, "null_handling" to false, "return_type" to false,
            "strict" to false,
        )
    }
}

// sqlglot: json.JSONArrayAgg(Expression, AggFunc)
class JSONArrayAgg(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "order" to false, "null_handling" to false, "return_type" to false,
            "strict" to false,
        )
    }
}

// sqlglot: json.JSONArrayAppend(Expression, Func) — _sql_names = ['JSON_ARRAY_APPEND'], is_var_len_args
class JSONArrayAppend(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to true,
        )
        private val SQL_NAMES = listOf("JSON_ARRAY_APPEND")
    }
}

// sqlglot: json.JSONArrayInsert(Expression, Func) — _sql_names = ['JSON_ARRAY_INSERT'], is_var_len_args
class JSONArrayInsert(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to true,
        )
        private val SQL_NAMES = listOf("JSON_ARRAY_INSERT")
    }
}

// sqlglot: json.JSONBExists(Expression, Func) — _sql_names = ['JSONB_EXISTS']
class JSONBExists(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "path" to true,
        )
        private val SQL_NAMES = listOf("JSONB_EXISTS")
    }
}

// sqlglot: json.JSONBObjectAgg(Expression, AggFunc)
class JSONBObjectAgg(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: json.JSONBool(Expression, Func)
class JSONBool(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: json.JSONExists(Expression, Func)
class JSONExists(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "path" to true, "passing" to false, "on_condition" to false,
            "from_dcolonqmark" to false,
        )
    }
}

// sqlglot: json.JSONExtractArray(Expression, Func) — _sql_names = ['JSON_EXTRACT_ARRAY']
class JSONExtractArray(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
        private val SQL_NAMES = listOf("JSON_EXTRACT_ARRAY")
    }
}

// sqlglot: json.JSONFormat(Expression, Func) — _sql_names = ['JSON_FORMAT']
class JSONFormat(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "options" to false, "is_json" to false, "to_json" to false,
        )
        private val SQL_NAMES = listOf("JSON_FORMAT")
    }
}

// sqlglot: json.JSONKeys(Expression, Func) — _sql_names = ['JSON_KEYS'], is_var_len_args
class JSONKeys(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false, "expressions" to false,
        )
        private val SQL_NAMES = listOf("JSON_KEYS")
    }
}

// sqlglot: json.JSONKeysAtDepth(Expression, Func)
class JSONKeysAtDepth(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false, "mode" to false,
        )
    }
}

// sqlglot: json.JSONObject(Expression, Func)
class JSONObject(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false, "null_handling" to false, "unique_keys" to false,
            "return_type" to false, "encoding" to false,
        )
    }
}

// sqlglot: json.JSONObjectAgg(Expression, AggFunc)
class JSONObjectAgg(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false, "null_handling" to false, "unique_keys" to false,
            "return_type" to false, "encoding" to false,
        )
    }
}

// sqlglot: json.JSONRemove(Expression, Func) — _sql_names = ['JSON_REMOVE'], is_var_len_args
class JSONRemove(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to true,
        )
        private val SQL_NAMES = listOf("JSON_REMOVE")
    }
}

// sqlglot: json.JSONSet(Expression, Func) — _sql_names = ['JSON_SET'], is_var_len_args
class JSONSet(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to true,
        )
        private val SQL_NAMES = listOf("JSON_SET")
    }
}

// sqlglot: json.JSONStripNulls(Expression, Func) — _sql_names = ['JSON_STRIP_NULLS']
class JSONStripNulls(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false, "include_arrays" to false,
            "remove_empty" to false,
        )
        private val SQL_NAMES = listOf("JSON_STRIP_NULLS")
    }
}

// sqlglot: json.JSONTable(Expression, Func)
class JSONTable(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "schema" to true, "path" to false, "error_handling" to false,
            "empty_handling" to false,
        )
    }
}

// sqlglot: json.JSONType(Expression, Func) — _sql_names = ['JSON_TYPE']
class JSONType(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
        private val SQL_NAMES = listOf("JSON_TYPE")
    }
}

// sqlglot: json.ObjectId(Expression, Func)
class ObjectId(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: json.ObjectInsert(Expression, Func)
class ObjectInsert(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "key" to true, "value" to true, "update_flag" to false,
        )
    }
}

// sqlglot: json.OpenJSON(Expression, Func)
class OpenJSON(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "path" to false, "expressions" to false,
        )
    }
}

// sqlglot: json.ParseJSON(Expression, Func) — _sql_names = ['PARSE_JSON', 'JSON_PARSE']
class ParseJSON(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false, "safe" to false,
        )
        private val SQL_NAMES = listOf("PARSE_JSON", "JSON_PARSE")
    }
}

// sqlglot: json.StripNullValue(Expression, Func)
class StripNullValue(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: json.JSONArrayContains(Expression, Binary, Predicate, Func) — _sql_names = ['JSON_ARRAY_CONTAINS']
class JSONArrayContains(initArgs: Args = emptyMap()) : Binary(initArgs), Predicate, Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "json_type" to false,
        )
        private val SQL_NAMES = listOf("JSON_ARRAY_CONTAINS")
    }
}

// sqlglot: json.JSONBContains(Expression, Binary, Func) — _sql_names = ['JSONB_CONTAINS']
class JSONBContains(initArgs: Args = emptyMap()) : Binary(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("JSONB_CONTAINS")
    }
}

// sqlglot: json.JSONBContainsAllTopKeys(Expression, Binary, Func)
class JSONBContainsAllTopKeys(initArgs: Args = emptyMap()) : Binary(initArgs), Func

// sqlglot: json.JSONBContainsAnyTopKeys(Expression, Binary, Func)
class JSONBContainsAnyTopKeys(initArgs: Args = emptyMap()) : Binary(initArgs), Func

// sqlglot: json.JSONBDeleteAtPath(Expression, Binary, Func)
class JSONBDeleteAtPath(initArgs: Args = emptyMap()) : Binary(initArgs), Func

// sqlglot: json.JSONBExtract(Expression, Binary, Func) — _sql_names = ['JSONB_EXTRACT']
class JSONBExtract(initArgs: Args = emptyMap()) : Binary(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("JSONB_EXTRACT")
    }
}

// sqlglot: json.JSONBExtractScalar(Expression, Binary, Func) — _sql_names = ['JSONB_EXTRACT_SCALAR']
class JSONBExtractScalar(initArgs: Args = emptyMap()) : Binary(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "json_type" to false,
        )
        private val SQL_NAMES = listOf("JSONB_EXTRACT_SCALAR")
    }
}

// sqlglot: json.JSONBPathExists(Expression, Binary, Predicate, Func)
class JSONBPathExists(initArgs: Args = emptyMap()) : Binary(initArgs), Predicate, Func

// sqlglot: json.JSONExtract(Expression, Binary, Func) — _sql_names = ['JSON_EXTRACT'], is_var_len_args
class JSONExtract(initArgs: Args = emptyMap()) : Binary(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "only_json_types" to false,
            "expressions" to false, "variant_extract" to false, "json_query" to false,
            "option" to false, "quote" to false, "on_condition" to false, "requires_json" to false,
            "emits" to false,
        )
        private val SQL_NAMES = listOf("JSON_EXTRACT")
    }
}

// sqlglot: json.JSONExtractScalar(Expression, Binary, Func) — _sql_names = ['JSON_EXTRACT_SCALAR'], is_var_len_args
class JSONExtractScalar(initArgs: Args = emptyMap()) : Binary(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "only_json_types" to false,
            "expressions" to false, "json_type" to false, "scalar_only" to false,
        )
        private val SQL_NAMES = listOf("JSON_EXTRACT_SCALAR")
    }
}
