// GENERATED FILE — DO NOT EDIT.
// Generated by tools/gen_ast_nodes.py from sqlglot v30.12.0-44-g93d16591.
// sqlglot is MIT-licensed, copyright Toby Mao. See reference/sqlglot/LICENSE.
package dev.brikk.house.sql.ast

// sqlglot.expressions.query: 110 generated classes.

// sqlglot: query.AddPartition(Expression)
class AddPartition(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "exists" to false, "location" to false,
        )
    }
}

// sqlglot: query.Analyze(Expression)
class Analyze(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "kind" to false, "this" to false, "options" to false, "mode" to false,
            "partition" to false, "expression" to false, "properties" to false,
        )
    }
}

// sqlglot: query.AnalyzeColumns(Expression)
class AnalyzeColumns(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.AnalyzeDelete(Expression)
class AnalyzeDelete(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "kind" to false,
        )
    }
}

// sqlglot: query.AnalyzeHistogram(Expression)
class AnalyzeHistogram(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to true, "expression" to false,
            "update_options" to false,
        )
    }
}

// sqlglot: query.AnalyzeListChainedRows(Expression)
class AnalyzeListChainedRows(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expression" to false,
        )
    }
}

// sqlglot: query.AnalyzeSample(Expression)
class AnalyzeSample(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "kind" to true, "sample" to true,
        )
    }
}

// sqlglot: query.AnalyzeStatistics(Expression)
class AnalyzeStatistics(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "kind" to true, "option" to false, "this" to false, "expressions" to false,
        )
    }
}

// sqlglot: query.AnalyzeValidate(Expression)
class AnalyzeValidate(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "kind" to true, "this" to false, "expression" to false,
        )
    }
}

// sqlglot: query.AnalyzeWith(Expression)
class AnalyzeWith(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true,
        )
    }
}

// sqlglot: query.AttachOption(Expression)
class AttachOption(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: query.BitString(Expression, Condition) — is_primitive
class BitString(initArgs: Args = emptyMap()) : Expression(initArgs), Condition {
    override val isPrimitive get() = true
}

// sqlglot: query.Block(Expression)
class Block(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true,
        )
    }
}

// sqlglot: query.ByteString(Expression, Condition) — is_primitive
class ByteString(initArgs: Args = emptyMap()) : Expression(initArgs), Condition {
    override val argTypes get() = ARG_TYPES
    override val isPrimitive get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "is_bytes" to false,
        )
    }
}

// sqlglot: query.Changes(Expression)
class Changes(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "information" to true, "at_before" to false, "end" to false,
        )
    }
}

// sqlglot: query.Cluster(Expression)
class Cluster(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true,
        )
    }
}

// sqlglot: query.ColumnDef(Expression)
class ColumnDef(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "kind" to false, "constraints" to false, "exists" to false,
            "position" to false, "default" to false, "output" to false,
        )
    }
}

// sqlglot: query.ColumnPosition(Expression)
class ColumnPosition(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "position" to true,
        )
    }
}

// sqlglot: query.ConditionalInsert(Expression)
class ConditionalInsert(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false, "else_" to false,
        )
    }
}

// sqlglot: query.Connect(Expression)
class Connect(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "start" to false, "connect" to true, "nocycle" to false,
        )
    }
}

// sqlglot: query.Cube(Expression)
class Cube(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false,
        )
    }
}

// sqlglot: query.DropPartition(Expression)
class DropPartition(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true, "exists" to false,
        )
    }
}

// sqlglot: query.EndStatement(Expression)
class EndStatement(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf()
    }
}

// sqlglot: query.Fetch(Expression)
class Fetch(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "direction" to false, "count" to false, "limit_options" to false,
        )
    }
}

// sqlglot: query.Final(Expression)
class Final(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.ForClause(Expression)
class ForClause(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "kind" to true, "expressions" to false,
        )
    }
}

// sqlglot: query.FormatJson(Expression)
class FormatJson(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.Get(Expression)
class Get(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "target" to true, "properties" to false,
        )
    }
}

// sqlglot: query.Grant(Expression)
class Grant(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "privileges" to true, "kind" to false, "securable" to true, "principals" to true,
            "grant_option" to false,
        )
    }
}

// sqlglot: query.GroupingSets(Expression)
class GroupingSets(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true,
        )
    }
}

// sqlglot: query.HexString(Expression, Condition) — is_primitive
class HexString(initArgs: Args = emptyMap()) : Expression(initArgs), Condition {
    override val argTypes get() = ARG_TYPES
    override val isPrimitive get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "is_integer" to false,
        )
    }
}

// sqlglot: query.HistoricalData(Expression)
class HistoricalData(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "kind" to true, "expression" to true,
        )
    }
}

// sqlglot: query.IfBlock(Expression)
class IfBlock(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "true" to true, "false" to false,
        )
    }
}

// sqlglot: query.Index(Expression)
class Index(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "table" to false, "unique" to false, "primary" to false,
            "amp" to false, "params" to false,
        )
    }
}

// sqlglot: query.IndexTableHint(Expression)
class IndexTableHint(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to false, "target" to false,
        )
    }
}

// sqlglot: query.InputOutputFormat(Expression)
class InputOutputFormat(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "input_format" to false, "output_format" to false,
        )
    }
}

// sqlglot: query.Into(Expression)
class Into(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "temporary" to false, "unlogged" to false, "bulk_collect" to false,
            "expressions" to false,
        )
    }
}

// sqlglot: query.Introducer(Expression)
class Introducer(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: query.JSON(Expression)
class JSON(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "with_" to false, "unique" to false,
        )
    }
}

// sqlglot: query.JSONColumnDef(Expression)
class JSONColumnDef(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "kind" to false, "path" to false, "nested_schema" to false,
            "ordinality" to false, "format_json" to false,
        )
    }
}

// sqlglot: query.JSONExtractQuote(Expression)
class JSONExtractQuote(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "option" to true, "scalar" to false,
        )
    }
}

// sqlglot: query.JSONKeyValue(Expression)
class JSONKeyValue(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: query.JSONPath(Expression)
class JSONPath(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true,
        )
    }
}

// sqlglot: query.JSONPathPart(Expression)
open class JSONPathPart(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf()
    }
}

// sqlglot: query.JSONSchema(Expression)
class JSONSchema(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true,
        )
    }
}

// sqlglot: query.JSONValue(Expression)
class JSONValue(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "path" to true, "returning" to false, "on_condition" to false,
        )
    }
}

// sqlglot: query.JSONValueArray(Expression, Func)
class JSONValueArray(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: query.Lambda(Expression)
class Lambda(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to true, "colon" to false,
        )
    }
}

// sqlglot: query.Lateral(Expression, UDTF)
class Lateral(initArgs: Args = emptyMap()) : Expression(initArgs), UDTF {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "view" to false, "outer" to false, "alias" to false,
            "cross_apply" to false, "ordinality" to false,
        )
    }
}

// sqlglot: query.LimitOptions(Expression)
class LimitOptions(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "percent" to false, "rows" to false, "with_ties" to false,
        )
    }
}

// sqlglot: query.Lock(Expression)
class Lock(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "update" to true, "expressions" to false, "wait" to false, "key" to false,
        )
    }
}

// sqlglot: query.MatchRecognize(Expression)
class MatchRecognize(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "partition_by" to false, "order" to false, "measures" to false, "rows" to false,
            "after" to false, "pattern" to false, "define" to false, "alias" to false,
        )
    }
}

// sqlglot: query.MatchRecognizeMeasure(Expression)
class MatchRecognizeMeasure(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "window_frame" to false,
        )
    }
}

// sqlglot: query.ModelAttribute(Expression)
class ModelAttribute(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: query.MultitableInserts(Expression)
class MultitableInserts(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true, "kind" to true, "source" to true,
        )
    }
}

// sqlglot: query.National(Expression) — is_primitive
class National(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val isPrimitive get() = true
}

// sqlglot: query.OnCondition(Expression)
class OnCondition(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "error" to false, "empty" to false, "null" to false,
        )
    }
}

// sqlglot: query.OpenJSONColumnDef(Expression)
class OpenJSONColumnDef(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "kind" to true, "path" to false, "as_json" to false,
        )
    }
}

// sqlglot: query.OverflowTruncateBehavior(Expression)
class OverflowTruncateBehavior(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "with_count" to true,
        )
    }
}

// sqlglot: query.Partition(Expression)
class Partition(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true, "subpartition" to false,
        )
    }
}

// sqlglot: query.PartitionId(Expression)
class PartitionId(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.PartitionRange(Expression)
class PartitionRange(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false, "expressions" to false,
        )
    }
}

// sqlglot: query.Pivot(Expression)
class Pivot(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "alias" to false, "expressions" to false, "fields" to false,
            "unpivot" to false, "using" to false, "group" to false, "columns" to false,
            "include_nulls" to false, "default_on_null" to false, "into" to false,
            "with_" to false, "identify_pivot_strings" to false, "prefixed_pivot_columns" to false,
            "pivot_column_naming" to false,
        )
    }
}

// sqlglot: query.PreWhere(Expression)
class PreWhere(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.Prior(Expression)
class Prior(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.ProjectionDef(Expression)
class ProjectionDef(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: query.Put(Expression)
class Put(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "target" to true, "properties" to false,
        )
    }
}

// sqlglot: query.Qualify(Expression)
class Qualify(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.QueryBand(Expression)
class QueryBand(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "scope" to false, "update" to false,
        )
    }
}

// sqlglot: query.QueryOption(Expression)
class QueryOption(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: query.RawString(Expression, Condition) — is_primitive
class RawString(initArgs: Args = emptyMap()) : Expression(initArgs), Condition {
    override val isPrimitive get() = true
}

// sqlglot: query.RecursiveWithSearch(Expression)
class RecursiveWithSearch(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "kind" to true, "this" to true, "expression" to true, "using" to false,
        )
    }
}

// sqlglot: query.ReplacePartition(Expression)
class ReplacePartition(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expression" to true, "source" to true,
        )
    }
}

// sqlglot: query.Return(Expression)
class Return(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.Revoke(Expression)
class Revoke(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "privileges" to true, "kind" to false, "securable" to true, "principals" to true,
            "grant_option" to false, "cascade" to false,
        )
    }
}

// sqlglot: query.Rollup(Expression)
class Rollup(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false,
        )
    }
}

// sqlglot: query.Schema(Expression)
class Schema(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "expressions" to false,
        )
    }
}

// sqlglot: query.ScopeResolution(Expression)
class ScopeResolution(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "expression" to true,
        )
    }
}

// sqlglot: query.SkipJSONColumn(Expression)
class SkipJSONColumn(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "regexp" to false, "expression" to true,
        )
    }
}

// sqlglot: query.StoredProcedure(Expression)
class StoredProcedure(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to false, "wrapped" to false,
        )
    }
}

// sqlglot: query.Stream(Expression)
class Stream(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.TableColumn(Expression)
class TableColumn(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.TableFromRows(Expression, UDTF)
class TableFromRows(initArgs: Args = emptyMap()) : Expression(initArgs), UDTF {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "alias" to false, "joins" to false, "pivots" to false,
            "sample" to false,
        )
    }
}

// sqlglot: query.TableSample(Expression)
class TableSample(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false, "method" to false, "bucket_numerator" to false,
            "bucket_denominator" to false, "bucket_field" to false, "percent" to false,
            "rows" to false, "size" to false, "seed" to false,
        )
    }
}

// sqlglot: query.Tag(Expression)
class Tag(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "prefix" to false, "postfix" to false,
        )
    }
}

// sqlglot: query.TranslateCharacters(Expression)
class TranslateCharacters(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "with_error" to false,
        )
    }
}

// sqlglot: query.UnicodeString(Expression, Condition)
class UnicodeString(initArgs: Args = emptyMap()) : Expression(initArgs), Condition {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "escape" to false,
        )
    }
}

// sqlglot: query.UnpivotColumns(Expression)
class UnpivotColumns(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to true,
        )
    }
}

// sqlglot: query.UsingData(Expression)
class UsingData(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.Values(Expression, UDTF)
class Values(initArgs: Args = emptyMap()) : Expression(initArgs), UDTF {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true, "alias" to false, "order" to false, "limit" to false,
            "offset" to false,
        )
    }
}

// sqlglot: query.Variadic(Expression)
class Variadic(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.Version(Expression)
class Version(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "kind" to true, "expression" to false,
        )
    }
}

// sqlglot: query.WhileBlock(Expression)
class WhileBlock(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "body" to true,
        )
    }
}

// sqlglot: query.WindowSpec(Expression)
class WindowSpec(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "kind" to false, "start" to false, "start_side" to false, "end" to false,
            "end_side" to false, "exclude" to false,
        )
    }
}

// sqlglot: query.WithFill(Expression)
class WithFill(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "from_" to false, "to" to false, "step" to false, "interpolate" to false,
        )
    }
}

// sqlglot: query.WithTableHint(Expression)
class WithTableHint(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true,
        )
    }
}

// sqlglot: query.XMLKeyValueOption(Expression)
class XMLKeyValueOption(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: query.XMLNamespace(Expression)
class XMLNamespace(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: query.Distribute(Order)
class Distribute(initArgs: Args = emptyMap()) : Order(initArgs)

// sqlglot: query.JSONPathFilter(JSONPathPart)
class JSONPathFilter(initArgs: Args = emptyMap()) : JSONPathPart(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true,
        )
    }
}

// sqlglot: query.JSONPathKey(JSONPathPart)
class JSONPathKey(initArgs: Args = emptyMap()) : JSONPathPart(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "quoted" to false,
        )
    }
}

// sqlglot: query.JSONPathRecursive(JSONPathPart)
class JSONPathRecursive(initArgs: Args = emptyMap()) : JSONPathPart(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false,
        )
    }
}

// sqlglot: query.JSONPathRoot(JSONPathPart)
class JSONPathRoot(initArgs: Args = emptyMap()) : JSONPathPart(initArgs)

// sqlglot: query.JSONPathScript(JSONPathPart)
class JSONPathScript(initArgs: Args = emptyMap()) : JSONPathPart(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true,
        )
    }
}

// sqlglot: query.JSONPathSelector(JSONPathPart)
class JSONPathSelector(initArgs: Args = emptyMap()) : JSONPathPart(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true,
        )
    }
}

// sqlglot: query.JSONPathSlice(JSONPathPart)
class JSONPathSlice(initArgs: Args = emptyMap()) : JSONPathPart(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "start" to false, "end" to false, "step" to false,
        )
    }
}

// sqlglot: query.JSONPathSubscript(JSONPathPart)
class JSONPathSubscript(initArgs: Args = emptyMap()) : JSONPathPart(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true,
        )
    }
}

// sqlglot: query.JSONPathUnion(JSONPathPart)
class JSONPathUnion(initArgs: Args = emptyMap()) : JSONPathPart(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true,
        )
    }
}

// sqlglot: query.JSONPathWildcard(JSONPathPart)
class JSONPathWildcard(initArgs: Args = emptyMap()) : JSONPathPart(initArgs)

// sqlglot: query.Sort(Order)
class Sort(initArgs: Args = emptyMap()) : Order(initArgs)
