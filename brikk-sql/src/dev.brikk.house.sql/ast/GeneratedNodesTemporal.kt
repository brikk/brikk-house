// GENERATED FILE — DO NOT EDIT.
// Generated by tools/gen_ast_nodes.py from sqlglot v30.12.0-44-g93d16591.
// sqlglot is MIT-licensed, copyright Toby Mao. See reference/sqlglot/LICENSE.
package dev.brikk.house.sql.ast

// sqlglot.expressions.temporal: 106 generated classes.

// sqlglot: temporal.AddMonths(Expression, Func)
class AddMonths(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "preserve_end_of_month" to false,
        )
    }
}

// sqlglot: temporal.ConvertTimezone(Expression, Func)
class ConvertTimezone(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "source_tz" to false, "target_tz" to true, "timestamp" to true, "options" to false,
        )
    }
}

// sqlglot: temporal.CurrentDate(Expression, Func)
class CurrentDate(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false,
        )
    }
}

// sqlglot: temporal.CurrentDatetime(Expression, Func)
class CurrentDatetime(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false,
        )
    }
}

// sqlglot: temporal.CurrentTime(Expression, Func)
class CurrentTime(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false,
        )
    }
}

// sqlglot: temporal.CurrentTimestamp(Expression, Func)
class CurrentTimestamp(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "sysdate" to false,
        )
    }
}

// sqlglot: temporal.CurrentTimestampLTZ(Expression, Func)
class CurrentTimestampLTZ(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf()
    }
}

// sqlglot: temporal.CurrentTimezone(Expression, Func)
class CurrentTimezone(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf()
    }
}

// sqlglot: temporal.Date(Expression, Func) — is_var_len_args
class Date(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "zone" to false, "expressions" to false,
        )
    }
}

// sqlglot: temporal.DateAdd(Expression, Func, IntervalOp)
class DateAdd(initArgs: Args = emptyMap()) : Expression(initArgs), Func, IntervalOp {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false,
        )
    }
}

// sqlglot: temporal.DateBin(Expression, Func, IntervalOp)
class DateBin(initArgs: Args = emptyMap()) : Expression(initArgs), Func, IntervalOp {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false, "zone" to false,
            "origin" to false,
        )
    }
}

// sqlglot: temporal.DateDiff(Expression, Func, TimeUnit) — _sql_names = ['DATEDIFF', 'DATE_DIFF']
class DateDiff(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false, "zone" to false,
            "big_int" to false, "date_part_boundary" to false,
        )
        private val SQL_NAMES = listOf("DATEDIFF", "DATE_DIFF")
    }
}

// sqlglot: temporal.DateFromParts(Expression, Func) — _sql_names = ['DATE_FROM_PARTS', 'DATEFROMPARTS']
class DateFromParts(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "year" to true, "month" to false, "day" to false, "allow_overflow" to false,
        )
        private val SQL_NAMES = listOf("DATE_FROM_PARTS", "DATEFROMPARTS")
    }
}

// sqlglot: temporal.DateFromUnixDate(Expression, Func)
class DateFromUnixDate(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.DateStrToDate(Expression, Func)
class DateStrToDate(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.DateSub(Expression, Func, IntervalOp)
class DateSub(initArgs: Args = emptyMap()) : Expression(initArgs), Func, IntervalOp {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false,
        )
    }
}

// sqlglot: temporal.DateToDateStr(Expression, Func)
class DateToDateStr(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.DateToDi(Expression, Func)
class DateToDi(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.DateTrunc(Expression, Func)
class DateTrunc(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "unit" to true, "this" to true, "zone" to false, "input_type_preserved" to false,
        )
    }
}

// sqlglot: temporal.Datetime(Expression, Func)
class Datetime(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: temporal.DatetimeAdd(Expression, Func, IntervalOp)
class DatetimeAdd(initArgs: Args = emptyMap()) : Expression(initArgs), Func, IntervalOp {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false,
        )
    }
}

// sqlglot: temporal.DatetimeDiff(Expression, Func, TimeUnit)
class DatetimeDiff(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false,
        )
    }
}

// sqlglot: temporal.DatetimeSub(Expression, Func, IntervalOp)
class DatetimeSub(initArgs: Args = emptyMap()) : Expression(initArgs), Func, IntervalOp {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false,
        )
    }
}

// sqlglot: temporal.DatetimeTrunc(Expression, Func, TimeUnit)
class DatetimeTrunc(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "unit" to true, "zone" to false,
        )
    }
}

// sqlglot: temporal.Day(Expression, Func)
class Day(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.DayOfMonth(Expression, Func) — _sql_names = ['DAY_OF_MONTH', 'DAYOFMONTH']
class DayOfMonth(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("DAY_OF_MONTH", "DAYOFMONTH")
    }
}

// sqlglot: temporal.DayOfWeek(Expression, Func) — _sql_names = ['DAY_OF_WEEK', 'DAYOFWEEK']
class DayOfWeek(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("DAY_OF_WEEK", "DAYOFWEEK")
    }
}

// sqlglot: temporal.DayOfWeekIso(Expression, Func) — _sql_names = ['DAYOFWEEK_ISO', 'ISODOW']
class DayOfWeekIso(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("DAYOFWEEK_ISO", "ISODOW")
    }
}

// sqlglot: temporal.DayOfYear(Expression, Func) — _sql_names = ['DAY_OF_YEAR', 'DAYOFYEAR']
class DayOfYear(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("DAY_OF_YEAR", "DAYOFYEAR")
    }
}

// sqlglot: temporal.Dayname(Expression, Func)
class Dayname(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "abbreviated" to false,
        )
    }
}

// sqlglot: temporal.DiToDate(Expression, Func)
class DiToDate(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.Extract(Expression, Func)
class Extract(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: temporal.FromISO8601Date(Expression, Func) — _sql_names = ['FROM_ISO8601_DATE']
class FromISO8601Date(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("FROM_ISO8601_DATE")
    }
}

// sqlglot: temporal.FromISO8601Timestamp(Expression, Func) — _sql_names = ['FROM_ISO8601_TIMESTAMP']
class FromISO8601Timestamp(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("FROM_ISO8601_TIMESTAMP")
    }
}

// sqlglot: temporal.FromISO8601TimestampNanos(Expression, Func) — _sql_names = ['FROM_ISO8601_TIMESTAMP_NANOS']
class FromISO8601TimestampNanos(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("FROM_ISO8601_TIMESTAMP_NANOS")
    }
}

// sqlglot: temporal.GapFill(Expression, Func)
class GapFill(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "ts_column" to true, "bucket_width" to true,
            "partitioning_columns" to false, "value_columns" to false, "origin" to false,
            "ignore_nulls" to false,
        )
    }
}

// sqlglot: temporal.GenerateDateArray(Expression, Func)
class GenerateDateArray(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "start" to true, "end" to true, "step" to false,
        )
    }
}

// sqlglot: temporal.GenerateTimestampArray(Expression, Func)
class GenerateTimestampArray(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "start" to true, "end" to true, "step" to true,
        )
    }
}

// sqlglot: temporal.GetExtract(Expression, Func)
class GetExtract(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: temporal.Hour(Expression, Func)
class Hour(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.JustifyDays(Expression, Func)
class JustifyDays(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.JustifyHours(Expression, Func)
class JustifyHours(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.JustifyInterval(Expression, Func)
class JustifyInterval(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.LastDay(Expression, Func, TimeUnit) — _sql_names = ['LAST_DAY', 'LAST_DAY_OF_MONTH']
class LastDay(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "unit" to false,
        )
        private val SQL_NAMES = listOf("LAST_DAY", "LAST_DAY_OF_MONTH")
    }
}

// sqlglot: temporal.Localtime(Expression, Func)
class Localtime(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false,
        )
    }
}

// sqlglot: temporal.Localtimestamp(Expression, Func)
class Localtimestamp(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false,
        )
    }
}

// sqlglot: temporal.MakeInterval(Expression, Func)
class MakeInterval(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "year" to false, "month" to false, "week" to false, "day" to false, "hour" to false,
            "minute" to false, "second" to false,
        )
    }
}

// sqlglot: temporal.Minute(Expression, Func)
class Minute(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.Month(Expression, Func)
class Month(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.Monthname(Expression, Func)
class Monthname(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "abbreviated" to false,
        )
    }
}

// sqlglot: temporal.MonthsBetween(Expression, Func)
class MonthsBetween(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "roundoff" to false,
        )
    }
}

// sqlglot: temporal.NextDay(Expression, Func)
class NextDay(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: temporal.ParseDatetime(Expression, Func)
class ParseDatetime(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to false, "zone" to false, "default_year" to false,
        )
    }
}

// sqlglot: temporal.ParseTime(Expression, Func)
class ParseTime(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to true,
        )
    }
}

// sqlglot: temporal.PreviousDay(Expression, Func)
class PreviousDay(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: temporal.Quarter(Expression, Func)
class Quarter(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.Second(Expression, Func)
class Second(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.StrToDate(Expression, Func)
class StrToDate(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to false, "safe" to false, "default_year" to false,
        )
    }
}

// sqlglot: temporal.StrToTime(Expression, Func)
class StrToTime(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to true, "zone" to false, "safe" to false,
            "target_type" to false, "default_year" to false,
        )
    }
}

// sqlglot: temporal.StrToUnix(Expression, Func)
class StrToUnix(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "format" to false,
        )
    }
}

// sqlglot: temporal.Systimestamp(Expression, Func)
class Systimestamp(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false,
        )
    }
}

// sqlglot: temporal.Time(Expression, Func)
class Time(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "zone" to false,
        )
    }
}

// sqlglot: temporal.TimeAdd(Expression, Func, TimeUnit)
class TimeAdd(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false,
        )
    }
}

// sqlglot: temporal.TimeDiff(Expression, Func, TimeUnit)
class TimeDiff(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false,
        )
    }
}

// sqlglot: temporal.TimeFromParts(Expression, Func) — _sql_names = ['TIME_FROM_PARTS', 'TIMEFROMPARTS']
class TimeFromParts(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "hour" to true, "min" to true, "sec" to true, "nano" to false, "fractions" to false,
            "precision" to false, "overflow" to false,
        )
        private val SQL_NAMES = listOf("TIME_FROM_PARTS", "TIMEFROMPARTS")
    }
}

// sqlglot: temporal.TimeSlice(Expression, Func, TimeUnit)
class TimeSlice(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to true, "kind" to false,
        )
    }
}

// sqlglot: temporal.TimeStrToDate(Expression, Func)
class TimeStrToDate(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.TimeStrToTime(Expression, Func)
class TimeStrToTime(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "zone" to false,
        )
    }
}

// sqlglot: temporal.TimeStrToUnix(Expression, Func)
class TimeStrToUnix(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.TimeSub(Expression, Func, TimeUnit)
class TimeSub(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false,
        )
    }
}

// sqlglot: temporal.TimeToStr(Expression, Func)
class TimeToStr(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to true, "culture" to false, "zone" to false,
        )
    }
}

// sqlglot: temporal.TimeToTimeStr(Expression, Func)
class TimeToTimeStr(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.TimeToUnix(Expression, Func)
class TimeToUnix(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.TimeTrunc(Expression, Func, TimeUnit)
class TimeTrunc(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "unit" to true, "zone" to false,
        )
    }
}

// sqlglot: temporal.Timestamp(Expression, Func)
class Timestamp(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "zone" to false, "with_tz" to false,
        )
    }
}

// sqlglot: temporal.TimestampAdd(Expression, Func, TimeUnit)
class TimestampAdd(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false,
        )
    }
}

// sqlglot: temporal.TimestampDiff(Expression, Func, TimeUnit) — _sql_names = ['TIMESTAMPDIFF', 'TIMESTAMP_DIFF']
class TimestampDiff(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false,
        )
        private val SQL_NAMES = listOf("TIMESTAMPDIFF", "TIMESTAMP_DIFF")
    }
}

// sqlglot: temporal.TimestampFromParts(Expression, Func) — _sql_names = ['TIMESTAMP_FROM_PARTS', 'TIMESTAMPFROMPARTS']
class TimestampFromParts(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "year" to false, "month" to false, "day" to false, "hour" to false, "min" to false,
            "sec" to false, "nano" to false, "zone" to false, "milli" to false, "this" to false,
            "expression" to false,
        )
        private val SQL_NAMES = listOf("TIMESTAMP_FROM_PARTS", "TIMESTAMPFROMPARTS")
    }
}

// sqlglot: temporal.TimestampLtzFromParts(Expression, Func) — _sql_names = ['TIMESTAMP_LTZ_FROM_PARTS', 'TIMESTAMPLTZFROMPARTS']
class TimestampLtzFromParts(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "year" to false, "month" to false, "day" to false, "hour" to false, "min" to false,
            "sec" to false, "nano" to false,
        )
        private val SQL_NAMES = listOf("TIMESTAMP_LTZ_FROM_PARTS", "TIMESTAMPLTZFROMPARTS")
    }
}

// sqlglot: temporal.TimestampSub(Expression, Func, TimeUnit)
class TimestampSub(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false,
        )
    }
}

// sqlglot: temporal.TimestampTrunc(Expression, Func, TimeUnit)
class TimestampTrunc(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "unit" to true, "zone" to false, "input_type_preserved" to false,
        )
    }
}

// sqlglot: temporal.TimestampTzFromParts(Expression, Func) — _sql_names = ['TIMESTAMP_TZ_FROM_PARTS', 'TIMESTAMPTZFROMPARTS']
class TimestampTzFromParts(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "year" to false, "month" to false, "day" to false, "hour" to false, "min" to false,
            "sec" to false, "nano" to false, "zone" to false,
        )
        private val SQL_NAMES = listOf("TIMESTAMP_TZ_FROM_PARTS", "TIMESTAMPTZFROMPARTS")
    }
}

// sqlglot: temporal.ToDays(Expression, Func)
class ToDays(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.TsOrDiToDi(Expression, Func)
class TsOrDiToDi(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.TsOrDsAdd(Expression, Func, TimeUnit)
class TsOrDsAdd(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false, "return_type" to false,
        )
    }
}

// sqlglot: temporal.TsOrDsDiff(Expression, Func, TimeUnit)
class TsOrDsDiff(initArgs: Args = emptyMap()) : Expression(initArgs), Func, TimeUnit {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "unit" to false,
        )
    }
}

// sqlglot: temporal.TsOrDsToDate(Expression, Func)
class TsOrDsToDate(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to false, "safe" to false,
        )
    }
}

// sqlglot: temporal.TsOrDsToDateStr(Expression, Func)
class TsOrDsToDateStr(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.TsOrDsToDatetime(Expression, Func)
class TsOrDsToDatetime(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.TsOrDsToTime(Expression, Func)
class TsOrDsToTime(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to false, "safe" to false,
        )
    }
}

// sqlglot: temporal.TsOrDsToTimestamp(Expression, Func)
class TsOrDsToTimestamp(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.UnixDate(Expression, Func)
class UnixDate(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.UnixMicros(Expression, Func)
class UnixMicros(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.UnixMillis(Expression, Func)
class UnixMillis(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.UnixSeconds(Expression, Func)
class UnixSeconds(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.UnixToStr(Expression, Func)
class UnixToStr(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "format" to false,
        )
    }
}

// sqlglot: temporal.UnixToTime(Expression, Func)
class UnixToTime(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "scale" to false, "zone" to false, "hours" to false,
            "minutes" to false, "format" to false, "target_type" to false,
        )
    }
}

// sqlglot: temporal.UnixToTimeStr(Expression, Func)
class UnixToTimeStr(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.UtcDate(Expression, Func)
class UtcDate(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf()
    }
}

// sqlglot: temporal.UtcTime(Expression, Func)
class UtcTime(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false,
        )
    }
}

// sqlglot: temporal.UtcTimestamp(Expression, Func)
class UtcTimestamp(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false,
        )
    }
}

// sqlglot: temporal.Week(Expression, Func)
class Week(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "mode" to false,
        )
    }
}

// sqlglot: temporal.WeekOfYear(Expression, Func) — _sql_names = ['WEEK_OF_YEAR', 'WEEKOFYEAR']
class WeekOfYear(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("WEEK_OF_YEAR", "WEEKOFYEAR")
    }
}

// sqlglot: temporal.Year(Expression, Func)
class Year(initArgs: Args = emptyMap()) : Expression(initArgs), Func

// sqlglot: temporal.YearOfWeek(Expression, Func) — _sql_names = ['YEAR_OF_WEEK', 'YEAROFWEEK']
class YearOfWeek(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("YEAR_OF_WEEK", "YEAROFWEEK")
    }
}

// sqlglot: temporal.YearOfWeekIso(Expression, Func) — _sql_names = ['YEAR_OF_WEEK_ISO', 'YEAROFWEEKISO']
class YearOfWeekIso(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("YEAR_OF_WEEK_ISO", "YEAROFWEEKISO")
    }
}
