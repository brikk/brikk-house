// GENERATED FILE — DO NOT EDIT.
// Generated by tools/gen_ast_nodes.py from sqlglot v30.12.0-44-g93d16591.
// sqlglot is MIT-licensed, copyright Toby Mao. See reference/sqlglot/LICENSE.
package dev.brikk.house.sql.ast

// sqlglot.expressions.aggregate: 67 generated classes.

// sqlglot: aggregate.AIAgg(Expression, AggFunc) — _sql_names = ['AI_AGG']
class AIAgg(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
        private val SQL_NAMES = listOf("AI_AGG")
    }
}

// sqlglot: aggregate.AISummarizeAgg(Expression, AggFunc) — _sql_names = ['AI_SUMMARIZE_AGG']
class AISummarizeAgg(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("AI_SUMMARIZE_AGG")
    }
}

// sqlglot: aggregate.AnyValue(Expression, AggFunc)
class AnyValue(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.ApproxPercentileAccumulate(Expression, AggFunc)
class ApproxPercentileAccumulate(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.ApproxPercentileCombine(Expression, AggFunc)
class ApproxPercentileCombine(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.ApproxPercentileEstimate(Expression, Func)
class ApproxPercentileEstimate(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "percentile" to true,
        )
    }
}

// sqlglot: aggregate.ApproxQuantiles(Expression, AggFunc)
class ApproxQuantiles(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.ApproxTopK(Expression, AggFunc)
class ApproxTopK(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false, "counters" to false,
        )
    }
}

// sqlglot: aggregate.ApproxTopKAccumulate(Expression, AggFunc)
class ApproxTopKAccumulate(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.ApproxTopKCombine(Expression, AggFunc)
class ApproxTopKCombine(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.ApproxTopKEstimate(Expression, Func)
class ApproxTopKEstimate(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.ApproxTopSum(Expression, AggFunc)
class ApproxTopSum(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "count" to true,
        )
    }
}

// sqlglot: aggregate.ApproximateSimilarity(Expression, AggFunc) — _sql_names = ['APPROXIMATE_SIMILARITY', 'APPROXIMATE_JACCARD_INDEX']
class ApproximateSimilarity(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("APPROXIMATE_SIMILARITY", "APPROXIMATE_JACCARD_INDEX")
    }
}

// sqlglot: aggregate.ArgMax(Expression, AggFunc) — _sql_names = ['ARG_MAX', 'ARGMAX', 'MAX_BY']
class ArgMax(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "count" to false,
        )
        private val SQL_NAMES = listOf("ARG_MAX", "ARGMAX", "MAX_BY")
    }
}

// sqlglot: aggregate.ArgMin(Expression, AggFunc) — _sql_names = ['ARG_MIN', 'ARGMIN', 'MIN_BY']
class ArgMin(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "count" to false,
        )
        private val SQL_NAMES = listOf("ARG_MIN", "ARGMIN", "MIN_BY")
    }
}

// sqlglot: aggregate.ArrayAgg(Expression, AggFunc)
class ArrayAgg(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "nulls_excluded" to false,
        )
    }
}

// sqlglot: aggregate.ArrayConcatAgg(Expression, AggFunc)
class ArrayConcatAgg(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.ArrayUnionAgg(Expression, AggFunc)
class ArrayUnionAgg(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.ArrayUniqueAgg(Expression, AggFunc)
class ArrayUniqueAgg(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.CountIf(Expression, AggFunc) — _sql_names = ['COUNT_IF', 'COUNTIF']
class CountIf(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "zero_on_all_null" to false,
        )
        private val SQL_NAMES = listOf("COUNT_IF", "COUNTIF")
    }
}

// sqlglot: aggregate.CovarPop(Expression, AggFunc)
class CovarPop(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: aggregate.CovarSamp(Expression, AggFunc)
class CovarSamp(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: aggregate.CumeDist(Expression, AggFunc) — is_var_len_args
class CumeDist(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false,
        )
    }
}

// sqlglot: aggregate.DenseRank(Expression, AggFunc) — is_var_len_args
class DenseRank(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false,
        )
    }
}

// sqlglot: aggregate.First(Expression, AggFunc)
class First(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.FirstValue(Expression, AggFunc)
class FirstValue(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.GroupConcat(Expression, AggFunc)
class GroupConcat(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "separator" to false, "on_overflow" to false,
        )
    }
}

// sqlglot: aggregate.Grouping(Expression, AggFunc) — is_var_len_args
class Grouping(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true,
        )
    }
}

// sqlglot: aggregate.GroupingId(Expression, AggFunc) — is_var_len_args
class GroupingId(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false,
        )
    }
}

// sqlglot: aggregate.Kurtosis(Expression, AggFunc)
class Kurtosis(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.Lag(Expression, AggFunc)
class Lag(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "offset" to false, "default" to false,
        )
    }
}

// sqlglot: aggregate.Last(Expression, AggFunc)
class Last(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.LastValue(Expression, AggFunc)
class LastValue(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.Lead(Expression, AggFunc)
class Lead(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "offset" to false, "default" to false,
        )
    }
}

// sqlglot: aggregate.LogicalAnd(Expression, AggFunc) — _sql_names = ['LOGICAL_AND', 'BOOL_AND', 'BOOLAND_AGG']
class LogicalAnd(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("LOGICAL_AND", "BOOL_AND", "BOOLAND_AGG")
    }
}

// sqlglot: aggregate.LogicalOr(Expression, AggFunc) — _sql_names = ['LOGICAL_OR', 'BOOL_OR', 'BOOLOR_AGG']
class LogicalOr(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("LOGICAL_OR", "BOOL_OR", "BOOLOR_AGG")
    }
}

// sqlglot: aggregate.Median(Expression, AggFunc)
class Median(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.Minhash(Expression, AggFunc) — is_var_len_args
class Minhash(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to true,
        )
    }
}

// sqlglot: aggregate.MinhashCombine(Expression, AggFunc)
class MinhashCombine(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.Mode(Expression, AggFunc)
class Mode(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "deterministic" to false,
        )
    }
}

// sqlglot: aggregate.NthValue(Expression, AggFunc)
class NthValue(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "offset" to true, "from_first" to false,
        )
    }
}

// sqlglot: aggregate.Ntile(Expression, AggFunc)
class Ntile(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false,
        )
    }
}

// sqlglot: aggregate.ObjectAgg(Expression, AggFunc)
class ObjectAgg(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.PercentRank(Expression, AggFunc) — is_var_len_args
class PercentRank(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false,
        )
    }
}

// sqlglot: aggregate.PercentileCont(Expression, AggFunc)
class PercentileCont(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.PercentileDisc(Expression, AggFunc)
class PercentileDisc(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.Quantile(Expression, AggFunc)
open class Quantile(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "quantile" to true,
        )
    }
}

// sqlglot: aggregate.Rank(Expression, AggFunc) — is_var_len_args
class Rank(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES
    override val isVarLenArgs get() = true

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false,
        )
    }
}

// sqlglot: aggregate.RegrAvgx(Expression, AggFunc)
class RegrAvgx(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: aggregate.RegrAvgy(Expression, AggFunc)
class RegrAvgy(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: aggregate.RegrCount(Expression, AggFunc)
class RegrCount(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.RegrIntercept(Expression, AggFunc)
class RegrIntercept(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.RegrR2(Expression, AggFunc)
class RegrR2(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.RegrSlope(Expression, AggFunc)
class RegrSlope(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to false,
        )
    }
}

// sqlglot: aggregate.RegrSxx(Expression, AggFunc)
class RegrSxx(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: aggregate.RegrSxy(Expression, AggFunc)
class RegrSxy(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: aggregate.RegrSyy(Expression, AggFunc)
class RegrSyy(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: aggregate.RegrValx(Expression, AggFunc)
class RegrValx(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: aggregate.RegrValy(Expression, AggFunc)
class RegrValy(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true,
        )
    }
}

// sqlglot: aggregate.Skewness(Expression, AggFunc)
class Skewness(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.Stddev(Expression, AggFunc) — _sql_names = ['STDDEV', 'STDEV']
class Stddev(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("STDDEV", "STDEV")
    }
}

// sqlglot: aggregate.StddevPop(Expression, AggFunc)
class StddevPop(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.StddevSamp(Expression, AggFunc)
class StddevSamp(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc

// sqlglot: aggregate.Variance(Expression, AggFunc) — _sql_names = ['VARIANCE', 'VARIANCE_SAMP', 'VAR_SAMP']
class Variance(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("VARIANCE", "VARIANCE_SAMP", "VAR_SAMP")
    }
}

// sqlglot: aggregate.VariancePop(Expression, AggFunc) — _sql_names = ['VARIANCE_POP', 'VAR_POP']
class VariancePop(initArgs: Args = emptyMap()) : Expression(initArgs), AggFunc {
    override val sqlNamesOverride get() = SQL_NAMES

    companion object {
        private val SQL_NAMES = listOf("VARIANCE_POP", "VAR_POP")
    }
}

// sqlglot: aggregate.ApproxQuantile(Quantile)
class ApproxQuantile(initArgs: Args = emptyMap()) : Quantile(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "quantile" to true, "accuracy" to false, "weight" to false,
            "error_tolerance" to false,
        )
    }
}

// sqlglot: aggregate.Corr(Expression, AggFunc, Binary)
class Corr(initArgs: Args = emptyMap()) : Binary(initArgs), AggFunc {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "null_on_zero_variance" to false,
        )
    }
}
