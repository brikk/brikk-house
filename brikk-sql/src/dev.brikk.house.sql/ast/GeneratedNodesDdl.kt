// GENERATED FILE — DO NOT EDIT.
// Generated by tools/gen_ast_nodes.py from sqlglot v30.12.0-44-g93d16591.
// sqlglot is MIT-licensed, copyright Toby Mao. See reference/sqlglot/LICENSE.
package dev.brikk.house.sql.ast

// sqlglot.expressions.ddl: 52 generated classes.

// sqlglot: ddl.Alter(Expression)
class Alter(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "kind" to true, "actions" to true, "exists" to false, "only" to false,
            "options" to false, "cluster" to false, "not_valid" to false, "check" to false,
            "cascade" to false, "iceberg" to false,
        )
    }
}

// sqlglot: ddl.AlterColumn(Expression)
class AlterColumn(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "dtype" to false, "collate" to false, "using" to false,
            "default" to false, "drop" to false, "comment" to false, "allow_null" to false,
            "visible" to false, "rename_to" to false,
        )
    }
}

// sqlglot: ddl.AlterDistStyle(Expression)
class AlterDistStyle(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: ddl.AlterIndex(Expression)
class AlterIndex(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "visible" to true,
        )
    }
}

// sqlglot: ddl.AlterModifySqlSecurity(Expression)
class AlterModifySqlSecurity(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true,
        )
    }
}

// sqlglot: ddl.AlterRename(Expression)
class AlterRename(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: ddl.AlterSession(Expression)
class AlterSession(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true, "unset" to false,
        )
    }
}

// sqlglot: ddl.AlterSet(Expression)
class AlterSet(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false, "option" to false, "tablespace" to false,
            "access_method" to false, "file_format" to false, "copy_options" to false,
            "tag" to false, "location" to false, "serde" to false,
        )
    }
}

// sqlglot: ddl.AlterSortKey(Expression)
class AlterSortKey(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "expressions" to false, "compound" to false,
        )
    }
}

// sqlglot: ddl.Attach(Expression)
class Attach(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "exists" to false, "expressions" to false,
        )
    }
}

// sqlglot: ddl.CharacterSet(Expression)
class CharacterSet(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "default" to false,
        )
    }
}

// sqlglot: ddl.Clone(Expression)
class Clone(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "shallow" to false, "copy" to false,
        )
    }
}

// sqlglot: ddl.Comment(Expression)
class Comment(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "kind" to true, "expression" to true, "exists" to false,
            "materialized" to false,
        )
    }
}

// sqlglot: ddl.Commit(Expression)
class Commit(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "chain" to false, "this" to false, "durability" to false,
        )
    }
}

// sqlglot: ddl.Comprehension(Expression)
class Comprehension(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expression" to true, "position" to false, "iterator" to true,
            "condition" to false,
        )
    }
}

// sqlglot: ddl.Create(Expression, DDL)
class Create(initArgs: Args = emptyMap()) : Expression(initArgs), DDL {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "with_" to false, "this" to true, "kind" to true, "expression" to false,
            "exists" to false, "properties" to false, "replace" to false, "refresh" to false,
            "unique" to false, "indexes" to false, "no_schema_binding" to false, "begin" to false,
            "clone" to false, "concurrently" to false, "clustered" to false,
        )
    }
}

// sqlglot: ddl.Declare(Expression)
class Declare(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true, "replace" to false,
        )
    }
}

// sqlglot: ddl.DeclareItem(Expression)
class DeclareItem(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "kind" to false, "default" to false,
        )
    }
}

// sqlglot: ddl.Describe(Expression)
class Describe(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "style" to false, "kind" to false, "properties" to false,
            "expressions" to false, "partition" to false, "format" to false, "as_json" to false,
        )
    }
}

// sqlglot: ddl.Detach(Expression)
class Detach(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "kind" to false, "exists" to false, "cluster" to false,
            "permanent" to false, "sync" to false,
        )
    }
}

// sqlglot: ddl.Drop(Expression)
class Drop(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "kind" to false, "expressions" to false, "exists" to false,
            "temporary" to false, "materialized" to false, "cascade" to false, "restrict" to false,
            "constraints" to false, "purge" to false, "cluster" to false, "concurrently" to false,
            "sync" to false, "iceberg" to false,
        )
    }
}

// sqlglot: ddl.DropPrimaryKey(Expression)
class DropPrimaryKey(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf()
    }
}

// sqlglot: ddl.Execute(Expression)
open class Execute(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to false, "return_status" to false,
        )
    }
}

// sqlglot: ddl.Heredoc(Expression)
class Heredoc(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "tag" to false,
        )
    }
}

// sqlglot: ddl.Install(Expression)
class Install(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "from_" to false, "force" to false,
        )
    }
}

// sqlglot: ddl.Kill(Expression)
class Kill(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "kind" to false,
        )
    }
}

// sqlglot: ddl.MacroOverload(Expression)
class MacroOverload(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to false, "is_table" to false,
        )
    }
}

// sqlglot: ddl.MacroOverloads(Expression)
class MacroOverloads(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true,
        )
    }
}

// sqlglot: ddl.MergeTreeTTL(Expression)
class MergeTreeTTL(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true, "where" to false, "group" to false, "aggregates" to false,
        )
    }
}

// sqlglot: ddl.MergeTreeTTLAction(Expression)
class MergeTreeTTLAction(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "delete" to false, "recompress" to false, "to_disk" to false,
            "to_volume" to false,
        )
    }
}

// sqlglot: ddl.ModifyColumn(Expression)
class ModifyColumn(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "rename_from" to false,
        )
    }
}

// sqlglot: ddl.NextValueFor(Expression, Func)
class NextValueFor(initArgs: Args = emptyMap()) : Expression(initArgs), Func {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "order" to false,
        )
    }
}

// sqlglot: ddl.Pragma(Expression)
class Pragma(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: ddl.RenameColumn(Expression)
class RenameColumn(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "to" to true, "exists" to false,
        )
    }
}

// sqlglot: ddl.RenameIndex(Expression)
class RenameIndex(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "to" to true,
        )
    }
}

// sqlglot: ddl.Rollback(Expression)
class Rollback(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "savepoint" to false, "this" to false,
        )
    }
}

// sqlglot: ddl.SequenceProperties(Expression)
class SequenceProperties(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "increment" to false, "minvalue" to false, "maxvalue" to false, "cache" to false,
            "start" to false, "owned" to false, "options" to false,
        )
    }
}

// sqlglot: ddl.Set(Expression)
class Set(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to false, "unset" to false, "tag" to false,
        )
    }
}

// sqlglot: ddl.SetItem(Expression)
class SetItem(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "expressions" to false, "kind" to false, "collate" to false,
            "global_" to false,
        )
    }
}

// sqlglot: ddl.Show(Expression)
class Show(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "history" to false, "terse" to false, "target" to false,
            "offset" to false, "starts_with" to false, "limit" to false, "from_" to false,
            "like" to false, "where" to false, "db" to false, "scope" to false,
            "scope_kind" to false, "full" to false, "mutex" to false, "query" to false,
            "channel" to false, "global_" to false, "log" to false, "position" to false,
            "types" to false, "privileges" to false, "for_table" to false, "for_group" to false,
            "for_user" to false, "for_role" to false, "into_outfile" to false, "json" to false,
            "iceberg" to false,
        )
    }
}

// sqlglot: ddl.Summarize(Expression)
class Summarize(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "table" to false,
        )
    }
}

// sqlglot: ddl.SwapTable(Expression)
class SwapTable(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: ddl.Transaction(Expression)
class Transaction(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "modes" to false, "mark" to false,
        )
    }
}

// sqlglot: ddl.TriggerEvent(Expression)
class TriggerEvent(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "columns" to false,
        )
    }
}

// sqlglot: ddl.TriggerExecute(Expression)
class TriggerExecute(initArgs: Args = emptyMap()) : Expression(initArgs)

// sqlglot: ddl.TriggerProperties(Expression)
class TriggerProperties(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "table" to true, "timing" to true, "events" to true, "execute" to true,
            "constraint" to false, "referenced_table" to false, "deferrable" to false,
            "initially" to false, "referencing" to false, "for_each" to false, "when" to false,
        )
    }
}

// sqlglot: ddl.TriggerReferencing(Expression)
class TriggerReferencing(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "old" to false, "new" to false,
        )
    }
}

// sqlglot: ddl.TruncateTable(Expression)
class TruncateTable(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "expressions" to true, "is_database" to false, "exists" to false, "only" to false,
            "cluster" to false, "identity" to false, "option" to false, "partition" to false,
        )
    }
}

// sqlglot: ddl.Undrop(Expression)
class Undrop(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "kind" to true, "rename" to false,
        )
    }
}

// sqlglot: ddl.Use(Expression)
class Use(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to false, "expressions" to false, "kind" to false,
        )
    }
}

// sqlglot: ddl.UserDefinedFunction(Expression)
class UserDefinedFunction(initArgs: Args = emptyMap()) : Expression(initArgs) {
    override val argTypes get() = ARG_TYPES

    companion object {
        private val ARG_TYPES = argTypesOf(
            "this" to true, "expressions" to false, "wrapped" to false,
        )
    }
}

// sqlglot: ddl.ExecuteSql(Execute)
class ExecuteSql(initArgs: Args = emptyMap()) : Execute(initArgs)
