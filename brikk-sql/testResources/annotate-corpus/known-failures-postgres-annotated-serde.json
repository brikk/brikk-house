{
 "note": "annotate_types gate ledger \u2014 every entry is a pre-existing parser gap, not a typing gap",
 "cases": [
  {
   "sql": "'x' 'y' 'z'",
   "reason": "pre-existing postgres parser gap (same entry in parser-corpus/postgres-parser-known-failures.json): ParseError: Adjacent string literals are not supported yet"
  },
  {
   "sql": "WITH t(c) AS (SELECT 1) SELECT * INTO UNLOGGED foo FROM (SELECT c AS c FROM t) AS temp",
   "reason": "pre-existing postgres parser gap (same entry in parser-corpus/postgres-parser-known-failures.json): ParseError: SELECT INTO is not supported yet"
  },
  {
   "sql": "SELECT * FROM ROWS FROM (FUNC1(col1, col2))",
   "reason": "pre-existing postgres parser gap (same entry in parser-corpus/postgres-parser-known-failures.json): ParseError: ROWS FROM is not supported yet"
  },
  {
   "sql": "SELECT * FROM ROWS FROM (FUNC1(col1) AS alias1(\"col1\" TEXT), FUNC2(col2) AS alias2(\"col2\" INT)) WITH ORDINALITY",
   "reason": "pre-existing postgres parser gap (same entry in parser-corpus/postgres-parser-known-failures.json): ParseError: ROWS FROM is not supported yet"
  },
  {
   "sql": "SELECT * FROM table1, ROWS FROM (FUNC1(col1) AS alias1(\"col1\" TEXT)) WITH ORDINALITY AS alias3(\"col3\" INT, \"col4\" TEXT)",
   "reason": "pre-existing postgres parser gap (same entry in parser-corpus/postgres-parser-known-failures.json): ParseError: Invalid expression / Unexpected token"
  },
  {
   "sql": "WITH RECURSIVE search_graph(id, link, data, depth) AS (SELECT g.id, g.link, g.data, 1 FROM graph AS g UNION ALL SELECT g.id, g.link, g.data, sg.depth + 1 FROM graph AS g, search_graph AS sg WHERE g.id = sg.link) CYCLE id SET is_cycle USING path SELECT * FROM search_graph",
   "reason": "pre-existing postgres parser gap (same entry in parser-corpus/postgres-parser-known-failures.json): ParseError: WITH "
  }
 ]
}
