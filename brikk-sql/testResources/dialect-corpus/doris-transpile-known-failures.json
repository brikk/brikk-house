{
    "cases": [
        {
            "case": "read|postgres|SELECT JSON_EXTRACT(CAST('{\"key\": 1}' AS JSONB), '$.key')",
            "reason": "expected `SELECT JSON_EXTRACT(CAST('{\"key\": 1}' AS JSONB), '$.key')` actual `SELECT JSON_UNQUOTE(JSON_EXTRACT(CAST('{\"key\": 1}' AS JSONB), '$.key'))`"
        }
    ]
}