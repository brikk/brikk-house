{
    "cases": [
        {
            "case": "write|duckdb, version=1.1.0|SELECT TRY_ELEMENT_AT(ARRAY(1, 2, 3), 2)",
            "reason": "expected `SELECT ([1, 2, 3])[2]` actual `SELECT [1, 2, 3][2]`"
        },
        {
            "case": "write|duckdb, version=1.1.0|SELECT TRY_ELEMENT_AT(MAP(1, 'a', 2, 'b'), 2)",
            "reason": "expected `SELECT (MAP([1, 2], ['a', 'b'])[2])[1]` actual `SELECT MAP([1, 2], ['a', 'b'])[2]`"
        },
        {
            "case": "write|bigquery|SELECT TO_UTC_TIMESTAMP('2016-08-31', 'Asia/Seoul')",
            "reason": "expected `SELECT DATETIME(TIMESTAMP(CAST('2016-08-31' AS DATETIME), 'Asia/Seoul'), 'UTC')` actual `SELECT CAST('2016-08-31' AS DATETIME) AT TIME ZONE 'Asia/Seoul' AT TIME ZONE 'UTC'`"
        },
        {
            "case": "read|bigquery|SELECT * FROM produce AS p PIVOT(SUM(p.sales) AS sales FOR quarter IN ('Q1' AS Q1, 'Q2' AS Q1))",
            "reason": "expected `SELECT * FROM produce AS p PIVOT(SUM(p.sales) AS sales FOR quarter IN ('Q1' AS Q1, 'Q2' AS Q1))` actual `SELECT * FROM produce AS p PIVOT(SUM(p.sales) AS sales FOR p.quarter IN ('Q1' AS Q1, 'Q2' AS Q1))`"
        },
        {
            "case": "write|bigquery|SELECT DATE_ADD(my_date_column, 1)",
            "reason": "expected `SELECT DATE_ADD(CAST(CAST(my_date_column AS DATETIME) AS DATE), INTERVAL 1 DAY)` actual `SELECT TS_OR_DS_ADD(my_date_column, 1, DAY)`"
        },
        {
            "case": "write|spark, version=3.0.0|STRING_AGG(x, ', ')",
            "reason": "expected `ARRAY_JOIN(COLLECT_LIST(x), ', ')` actual `LISTAGG(x, ', ')`"
        },
        {
            "case": "write|spark, version=3.0.0|LISTAGG(x, ', ')",
            "reason": "expected `ARRAY_JOIN(COLLECT_LIST(x), ', ')` actual `LISTAGG(x, ', ')`"
        },
        {
            "case": "write|bigquery|SELECT EXPLODE(x) FROM tbl",
            "reason": "expected `SELECT IF(pos = pos_2, col, NULL) AS col FROM tbl CROSS JOIN UNNEST(GENERATE_ARRAY(0, GREATEST(ARRAY_LENGTH(x)) - 1)) AS pos CROSS JOIN UNNEST(x) AS col WITH OFFSET AS pos_2 WHERE pos = pos_2 OR (pos > (ARRAY_LENGTH(x) - 1) AND pos_2 = (ARRAY_LENGTH(x) - 1))` actual `SELECT EXPLODE(x) FROM tbl`"
        },
        {
            "case": "write|bigquery|SELECT EXPLODE(col) FROM _u",
            "reason": "expected `SELECT IF(pos = pos_2, col_2, NULL) AS col_2 FROM _u CROSS JOIN UNNEST(GENERATE_ARRAY(0, GREATEST(ARRAY_LENGTH(col)) - 1)) AS pos CROSS JOIN UNNEST(col) AS col_2 WITH OFFSET AS pos_2 WHERE pos = pos_2 OR (pos > (ARRAY_LENGTH(col) - 1) AND pos_2 = (ARRAY_LENGTH(col) - 1))` actual `SELECT EXPLODE(col) FROM _u`"
        },
        {
            "case": "write|bigquery|SELECT EXPLODE(ARRAY(1, 2))",
            "reason": "expected `SELECT IF(pos = pos_2, col, NULL) AS col FROM UNNEST(GENERATE_ARRAY(0, GREATEST(ARRAY_LENGTH([1, 2])) - 1)) AS pos CROSS JOIN UNNEST([1, 2]) AS col WITH OFFSET AS pos_2 WHERE pos = pos_2 OR (pos > (ARRAY_LENGTH([1, 2]) - 1) AND pos_2 = (ARRAY_LENGTH([1, 2]) - 1))` actual `SELECT EXPLODE([1, 2])`"
        },
        {
            "case": "write|bigquery|SELECT POSEXPLODE(ARRAY(2, 3)) AS x",
            "reason": "expected `SELECT IF(pos = pos_2, x, NULL) AS x, IF(pos = pos_2, pos_2, NULL) AS pos_2 FROM UNNEST(GENERATE_ARRAY(0, GREATEST(ARRAY_LENGTH([2, 3])) - 1)) AS pos CROSS JOIN UNNEST([2, 3]) AS x WITH OFFSET AS pos_2 WHERE pos = pos_2 OR (pos > (ARRAY_LENGTH([2, 3]) - 1) AND pos_2 = (ARRAY_LENGTH([2, 3]) - 1))` actual `SELECT POSEXPLODE([2, 3]) AS x`"
        },
        {
            "case": "write|bigquery|SELECT POSEXPLODE(ARRAY(2, 3)), EXPLODE(ARRAY(4, 5, 6)) FROM tbl",
            "reason": "expected `SELECT IF(pos = pos_2, col, NULL) AS col, IF(pos = pos_2, pos_2, NULL) AS pos_2, IF(pos = pos_3, col_2, NULL) AS col_2 FROM tbl CROSS JOIN UNNEST(GENERATE_ARRAY(0, GREATEST(ARRAY_LENGTH([2, 3]), ARRAY_LENGTH([4, 5, 6])) - 1)) AS pos CROSS JOIN UNNEST([2, 3]) AS col WITH OFFSET AS pos_2 CROSS JOIN UNNEST([4, 5, 6]) AS col_2 WITH OFFSET AS pos_3 WHERE (pos = pos_2 OR (pos > (ARRAY_LENGTH([2, 3]) - 1) AND pos_2 = (ARRAY_LENGTH([2, 3]) - 1))) AND (pos = pos_3 OR (pos > (ARRAY_LENGTH([4, 5, 6]) - 1) AND pos_3 = (ARRAY_LENGTH([4, 5, 6]) - 1)))` actual `SELECT POSEXPLODE([2, 3]), EXPLODE([4, 5, 6]) FROM tbl`"
        }
    ]
}