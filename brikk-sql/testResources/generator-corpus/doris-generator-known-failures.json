{
    "sqlglot_version": "v30.17.0-93-gdcc36544a",
    "cases": [
        {
            "sql": "CREATE MATERIALIZED VIEW test_table (c1 INT, c2 INT) KEY (c1)",
            "reason": "output mismatch: expected `CREATE MATERIALIZED VIEW test_table (c1 INT, c2 INT) KEY (c1)` actual `CREATE MATERIALIZED VIEW test_table (c1, c2) KEY (c1)`"
        },
        {
            "sql": "CREATE TABLE test_table (c1 INT, c2 DATE) PARTITION BY (c2)",
            "reason": "output mismatch: expected `CREATE TABLE test_table (c1 INT, c2 DATE) PARTITION BY (c2)` actual `CREATE TABLE test_table (c1 INT, c2 DATE) PARTITION BY (c2) ()`"
        },
        {
            "sql": "CREATE TABLE test_table (c1 INT, c2 DATE) PARTITION BY (c1, c2)",
            "reason": "output mismatch: expected `CREATE TABLE test_table (c1 INT, c2 DATE) PARTITION BY (c1, c2)` actual `CREATE TABLE test_table (c1 INT, c2 DATE) PARTITION BY (c1, c2) ()`"
        },
        {
            "sql": "CREATE TABLE test_table (c1 INT, c2 DATE) PARTITION BY (DATE_TRUNC(c2, 'MONTH'))",
            "reason": "output mismatch: expected `CREATE TABLE test_table (c1 INT, c2 DATE) PARTITION BY (DATE_TRUNC(c2, 'MONTH'))` actual `CREATE TABLE test_table (c1 INT, c2 DATE) PARTITION BY RANGE (DATE_TRUNC(c2, 'MONTH')) ()`"
        }
    ]
}