# StarRocks native-grammar engine verification

Generated by `tools/verify_starrocks_engine.py` against the pinned live engine
`starrocks/allin1-ubuntu:4.1.4` (`current_version()` = 4.1.4-4a9848e).

Each brikk-emitted StarRocks SELECT from the dialect corpus is sent to the live
engine parser/analyzer. **PARSE_ACCEPTED** = the grammar accepted it (any error is
a missing-table/column analysis error, since the corpus uses schema-less literal
table names). **EXECUTED** = ran to completion. **SYNTAX_REJECT** = a real parser
rejection (a genuine grammar bug worth fixing).

## Summary

| verdict | count |
|---|---|
| EXECUTED | 21 |
| PARSE_ACCEPTED | 27 |

Corpus SELECTs verified: 37; representative mappings: 11.

## Representative function mappings

| mapping | sql | verdict |
|---|---|---|
| ArgMax->MAX_BY | `SELECT MAX_BY(a, b) FROM t` | PARSE_ACCEPTED |
| ArrayToString->ARRAY_JOIN | `SELECT ARRAY_JOIN([1, 2], '_') FROM t` | PARSE_ACCEPTED |
| RegexpLike->REGEXP | `SELECT a REGEXP 'x' FROM t` | PARSE_ACCEPTED |
| Flatten->ARRAY_FLATTEN | `SELECT ARRAY_FLATTEN([[1, 2], [3]]) FROM t` | PARSE_ACCEPTED |
| ArrayContainsAll | `SELECT ARRAY_CONTAINS_ALL([1, 2, 3], [1, 2]) FROM t` | PARSE_ACCEPTED |
| StDistance->ST_Distance_Sphere | `SELECT ST_Distance_Sphere(ST_X(p1), ST_Y(p1), ST_X(p2), ST_Y(p2)) FROM t` | PARSE_ACCEPTED |
| TimestampTrunc->DATE_TRUNC | `SELECT DATE_TRUNC('MONTH', ts) FROM t` | PARSE_ACCEPTED |
| UnixToStr->FROM_UNIXTIME | `SELECT FROM_UNIXTIME(n, '%Y') FROM t` | PARSE_ACCEPTED |
| Map variadic | `SELECT MAP('k1', 1, 'k2', 2)` | EXECUTED |
| TableFromRows/GENERATE_SERIES | `SELECT * FROM TABLE(GENERATE_SERIES(0, 10))` | EXECUTED |
| PARSE_JSON | `SELECT PARSE_JSON('{"a": 1}')` | EXECUTED |

## Corpus SELECT results

| sql | verdict |
|---|---|
| `SELECT ARRAY_JOIN([1, 3, 5, NULL], '_', 'NULL')` | EXECUTED |
| `SELECT ARRAY_JOIN([1, 3, 5, NULL], '_')` | EXECUTED |
| `SELECT ARRAY_AGG(a) FROM x` | PARSE_ACCEPTED |
| `SELECT ST_POINT(10, 20)` | EXECUTED |
| `SELECT ST_DISTANCE_SPHERE(10.1, 20.2, 30.3, 40.4)` | EXECUTED |
| `SELECT t1.id FROM t1 LEFT ANTI JOIN t2 ON t1.id = t2.id` | PARSE_ACCEPTED |
| `SELECT t1.id FROM t1 LEFT SEMI JOIN t2 ON t1.id = t2.id` | PARSE_ACCEPTED |
| `SELECT MIN_BY(name, ts) FROM t` | PARSE_ACCEPTED |
| `SELECT MAX_BY(name, ts) FROM t` | PARSE_ACCEPTED |
| `SELECT MIN_BY(x, y) OVER (PARTITION BY z) FROM t` | PARSE_ACCEPTED |
| `SELECT MAP('a', 1, 'b', 2)` | EXECUTED |
| `SELECT MAP('a', 1, 'b', 2)['a']` | EXECUTED |
| `SELECT ARRAY_CONTAINS_ALL([1, 2, 3], [1, 2])` | EXECUTED |
| `SELECT TRIM('  hi  ')` | EXECUTED |
| `SELECT TRIM('--hi--', '-')` | EXECUTED |
| `SELECT LTRIM('xxhi', 'x')` | EXECUTED |
| `SELECT RTRIM('hixx', 'x')` | EXECUTED |
| `SELECT a, b FROM (SELECT a AS a, b AS b, ROW_NUMBER() OVER (PARTITION BY a ORDER BY c DESC) AS _row_number FROM x) AS _t WHERE _row_number = 1` | PARSE_ACCEPTED |
| `SELECT generate_series FROM TABLE(GENERATE_SERIES(0, 10, 2)) AS t` | EXECUTED |
| `SELECT x FROM TABLE(GENERATE_SERIES(0, 10)) AS t(x)` | EXECUTED |
| `SELECT * FROM t, GENERATE_SERIES(t.s, t.e)` | PARSE_ACCEPTED |
| `SELECT CAST(`a`.`b` AS INT) FROM foo` | PARSE_ACCEPTED |
| `SELECT APPROX_COUNT_DISTINCT(a) FROM x` | PARSE_ACCEPTED |
| `SELECT [1, 2, 3]` | EXECUTED |
| `SELECT CAST(PARSE_JSON(fieldvalue) -> '00000000-0000-0000-0000-00000000' AS VARCHAR) AS `code` FROM (SELECT '{"00000000-0000-0000-0000-00000000":"code01"}') AS t(fieldvalue)` | EXECUTED |
| `SELECT `text` FROM example_table` | PARSE_ACCEPTED |
| `SELECT DATE_DIFF('SECOND', '2010-11-30 23:59:59', '2010-11-30 20:58:59')` | EXECUTED |
| `SELECT DATE_DIFF('MINUTE', '2010-11-30 23:59:59', '2010-11-30 20:58:59')` | EXECUTED |
| `SELECT DATE_ADD(x, INTERVAL '3' DAY)` | PARSE_ACCEPTED |
| `SELECT DATE_SUB(x, INTERVAL '3' MONTH)` | PARSE_ACCEPTED |
| `SELECT DATE_SUB(x, INTERVAL 3 DAY)` | PARSE_ACCEPTED |
| `SELECT DATE_ADD(x, INTERVAL 7 DAY)` | PARSE_ACCEPTED |
| `SELECT DATE_ADD(x, INTERVAL 7 DAY)` | PARSE_ACCEPTED |
| `SELECT DATE_SUB(x, INTERVAL 7 DAY)` | PARSE_ACCEPTED |
| `SELECT student, score, t.unnest FROM tests CROSS JOIN LATERAL UNNEST(scores) AS t(unnest)` | PARSE_ACCEPTED |
| `SELECT a BETWEEN 1 AND 5 FROM t` | PARSE_ACCEPTED |
| `SELECT * FROM TABLE(GENERATE_SERIES(0, 10))` | EXECUTED |
