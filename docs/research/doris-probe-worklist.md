# Doris semantic probe worklist

Hand-off list for live differential probes of **duckdb→doris** and **trino→doris**
same-name (bucket A) functions. Bucket A means the name exists in both catalogs —
that is exactly where silent semantic divergence hides; identical names do **not**
imply identical semantics (`gap-report.json` warning).

## Methodology reference

Follow the ducklake probe methodology documented in
[function-semantics-trino-duckdb.md — verification methodology]
(function-semantics-trino-duckdb.md#their-verification-methodology-what-verified-means):

1. **Live differential testing** against both engines (not doc-reading).
2. **Probe classes** that emit result tables for the same inputs on source and Doris;
   paste findings into a short REPORT, then delete the throwaway probe.
3. **Divergence-pressure corpus** — NULL algebra, Unicode edge cases (ZWJ emoji,
   Turkish İ, German ß), timezone/session-zone traps, leap days, negative epochs,
   type-coercion corners.
4. **Discipline:** when in doubt, mark `unclear` / do not claim equivalence.

Prior trino↔duckdb verdicts (from `trino-duckdb-hazards.json`) are **hints only** —
re-check every annotated function against Doris; a prior IDENTICAL does not mean
Doris matches either engine.

## Expected deliverable format

Fill `brikk-sql/testResources/semantics/{duckdb,trino}-doris-hazards.json` `pairs`
arrays with one object per probed function (same schema as `trino-duckdb-hazards.json`):

```json
{
  "duckdb": "<source-side name>",   // or "trino" for the trino-doris file
  "doris": "<doris-side name>",
  "verdict": "identical" | "divergent" | "conditionally-equivalent" | "no-equivalent" | "unclear",
  "hazard": "<human-readable finding or null>",
  "areas": ["null", "unicode", "..."],
  "provenance": "<REPORT-file.md#anchor or probe-id>"
}
```

Then regenerate the shipped registry:

```bash
python3 tools/generate_hazards_registry.py
```

Sort order below: **prior-DIVERGENT first** (highest suspicion), then other prior
evidence (by severity), then functions with no trino↔duckdb prior (alphabetical).

_Generated by `tools/generate_probe_worklist.py` from `gap-report.json` + `trino-duckdb-hazards.json`. Re-run is byte-deterministic._

## duckdb → doris

Bucket A same-name candidates: **181**. With prior trino↔duckdb evidence: **99** (of which **16** prior-DIVERGENT).

| function | kinds | arity note | prior trino↔duckdb verdict + hazard | suggested probe areas |
| --- | --- | --- | --- | --- |
| `ascii` | scalar | arity-compatible | **DIVERGENT** — Trino codepoint requires varchar(1); DuckDB unicode takes any varchar and uses first char — 'NOT 1:1' | string, unicode |
| `bit_count` | scalar | arity-compatible | **DIVERGENT** — Trino requires explicit bit-width argument; DuckDB infers from type — do not translate without matching width | numeric |
| `concat` | scalar | arity-compatible | **DIVERGENT** — Probe-verified on every NULL-bearing case: concat('a',NULL,'c','d')='acd', concat(NULL,'b','c')='bc', concat(NULL,NULL)='' in DuckDB; Trino returns NULL in a... | string, null |
| `fmod` | macro | arity-compatible | **DIVERGENT** — Trino float % uses IEEE remainder-ish semantics; DuckDB has fmod — 'do not push float % until aligned'; translator gates by arg type | numeric |
| `format` | scalar | arity-compatible | **DIVERGENT** — Format specifications differ entirely (Trino: Java Formatter; DuckDB: fmt/printf) — do not translate by name | string |
| `greatest` | scalar | arity-unknown | **DIVERGENT** — NULL handling differs: Trino returns NULL if ANY arg is NULL; DuckDB SKIPS NULLs — never translate by name when args may be NULL | null |
| `least` | scalar | arity-unknown | **DIVERGENT** — NULL handling differs: Trino returns NULL if ANY arg is NULL; DuckDB SKIPS NULLs — never translate by name when args may be NULL | null |
| `lower` | scalar | arity-compatible | **DIVERGENT** — Probe-verified: Turkish 'İ' -> DuckDB 'i' vs Trino 'i'+U+0307 (full case folding); ASCII and most non-ASCII aligned. Needed native ICU extension for parity | string, unicode |
| `ltrim` | scalar | arity-compatible | **DIVERGENT** — Same whitespace-set divergence as trim (tab/LF/CR/FF/VT not stripped by DuckDB default) | string, unicode |
| `regexp_extract_all` | scalar | arity-compatible | **DIVERGENT** — 'Empty-match handling differs; verify before pushing' | regex |
| `regexp_replace` | scalar | arity-compatible | **DIVERGENT** — Trino replaces ALL matches by default; DuckDB replaces first match unless 'g' flag — parity macro forces 'g' | regex |
| `reverse` | scalar | arity-compatible | **DIVERGENT** — Probe-verified: DuckDB reverse is GRAPHEME-cluster-aware, Trino code-point-only — decomposed 'café' reverses keeping e+U+0301 together in DuckDB; ZWJ family ... | string, unicode |
| `rtrim` | scalar | arity-compatible | **DIVERGENT** — Same whitespace-set divergence as trim | string, unicode |
| `split_part` | macro | arity-compatible | **DIVERGENT** — Out-of-bounds index: Trino returns NULL, DuckDB returns empty string — do not translate by name without NULLIF wrap | string, null |
| `trim` | scalar | arity-compatible | **DIVERGENT** — Probe-verified: DuckDB bare trim strips space + EM SPACE but NOT tab/LF/CR/FF/VT; Trino (Java Character.isWhitespace) strips all of those; both leave NBSP/ZW... | string, unicode |
| `upper` | scalar | arity-compatible | **DIVERGENT** — Probe-verified: German 'ß' -> DuckDB 'ẞ' (U+1E9E) vs Trino 'SS' (Java full folding, length-changing); ASCII aligned. Needed native ICU extension | string, unicode |
| `round` | scalar | arity-compatible | **UNCLEAR** — 'Trino: round is half-up. DuckDB: round is half-away-from-zero (since 0.10) — verify per version... Do NOT push when d > 0 until verified.' DuckDB also has r... | numeric |
| `any_value` | aggregate | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Order-dependence undefined in both unless ORDER BY used | aggregate |
| `approx_count_distinct` | aggregate | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Rename; HLL sketch STATES are not interchangeable across engines — only cardinality results comparable, Trino error-bound arg has no DuckDB counterpart | aggregate, extension |
| `current_date` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Both session-local; do not push if behavior must match a specific timezone definition | datetime, timezone |
| `date_trunc` | scalar | arity-unknown | **CONDITIONALLY_EQUIVALENT** — Aligned for intersection unit set (second..year) but DuckDB always returns TIMESTAMP even for DATE input while Trino preserves input type; numeric comparison... | datetime |
| `hour` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Probe-verified equivalent ONLY with SET TimeZone = Trino session zone before evaluation; without it, year(tz_col)=2024 can silently return 2025 rows (Asia/Si... | datetime, timezone |
| `isinf` | scalar | arity-compatible; sqlglot→is_inf | **CONDITIONALLY_EQUIVALENT** — Rename only, behavior aligned; NaN=NaN treated true for grouping/distinct in both but flagged 'verify in pushdown tests' | numeric |
| `isnan` | scalar | arity-compatible; sqlglot→is_nan | **CONDITIONALLY_EQUIVALENT** — Rename only, behavior aligned; NaN=NaN treated true for grouping/distinct in both but flagged 'verify in pushdown tests' | numeric |
| `json_extract` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Path syntax differs (Trino restricted JSONPath subset vs DuckDB JSONPath-ish with wildcards); safe only for simple $.a.b property paths; DuckDB side lives in... | string, extension |
| `json_extract_string` | scalar | arity-compatible; sqlglot→json_extract | **CONDITIONALLY_EQUIVALENT** — Rename, same intent; same path-syntax caveat | string, extension |
| `kurtosis` | aggregate | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Trino kurtosis = sample excess; DuckDB has both sample and pop variants — 'push only with explicit match' | aggregate, numeric |
| `map_entries` | scalar | arity-unknown | **CONDITIONALLY_EQUIVALENT** — Element type differs: Trino row(K,V) vs DuckDB struct(key,value); needs type-layer mapping | string |
| `octet_length` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Same semantics, different name; translator must pick length vs octet_length by Trino argument type | string |
| `random` | scalar | arity-compatible; sqlglot→rand | **CONDITIONALLY_EQUIVALENT** — Non-deterministic (never pushable); Trino bounded forms random(n), random(m,n) have no DuckDB equivalent | numeric |
| `substring` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Verified aligned on code-point unit (both split combining marks/ZWJ/flags at codepoint boundaries); negative start counts from end in both; but start=0 unver... | string, unicode |
| `uuid` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Non-deterministic — same distribution but never pushable/comparable | numeric |
| `year` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Probe-verified equivalent ONLY with SET TimeZone = Trino session zone before evaluation; without it, year(tz_col)=2024 can silently return 2025 rows (Asia/Si... | datetime, timezone |
| `abs` | scalar | arity-unknown | **IDENTICAL** — abs(MIN_INT): both throw | numeric |
| `avg` | aggregate | arity-compatible | **IDENTICAL** — Skips NULLs in both | aggregate, null |
| `bit_length` | scalar | arity-compatible | **IDENTICAL** | string |
| `bool_and` | aggregate | arity-compatible; sqlglot→min | **IDENTICAL** | aggregate |
| `bool_or` | aggregate | arity-compatible; sqlglot→max | **IDENTICAL** | aggregate |
| `cardinality` | scalar | arity-compatible | **IDENTICAL** | aggregate |
| `cbrt` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `ceil` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `concat_ws` | scalar | arity-compatible | **IDENTICAL** — Probe-verified aligned on ALL NULL shapes: NULL elements skipped by both, NULL separator -> NULL in both, all-NULL elements -> '' in both; Unicode separator OK | string, unicode, null |
| `corr` | aggregate | arity-compatible | **IDENTICAL** | aggregate, numeric |
| `cosh` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `count` | aggregate | arity-compatible | **IDENTICAL** — Both skip NULLs for count(expr) | aggregate, null |
| `covar_pop` | aggregate | arity-compatible | **IDENTICAL** | aggregate, numeric |
| `covar_samp` | aggregate | arity-compatible | **IDENTICAL** | aggregate, numeric |
| `cume_dist` | aggregate | arity-unknown | **IDENTICAL** — Trino 481 docs claim '-> bigint' but it actually returns double like DuckDB — doc bug, trust the type system | aggregate, numeric |
| `day` | scalar | arity-compatible | **IDENTICAL** — Aligned on DATE and TIMESTAMP input | datetime |
| `dayofyear` | scalar | arity-compatible | **IDENTICAL** — Rename; pinned by leap-day fixture '2024-02-29' -> 60 | datetime |
| `degrees` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `exp` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `floor` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `from_base64` | scalar | arity-compatible | **IDENTICAL** | string |
| `hex` | scalar | arity-compatible | **IDENTICAL** — Rename only | string |
| `histogram` | aggregate | arity-compatible | **IDENTICAL** — Counts by value in both | aggregate |
| `last_day` | scalar | arity-compatible | **IDENTICAL** — Rename; pinned leap-Feb '2024-02-15' -> '2024-02-29' and '1900-02-15' -> '1900-02-28' | datetime |
| `length` | scalar | arity-compatible; sqlglot→char_length | **IDENTICAL** — Rename; NULL -> NULL in both | aggregate |
| `levenshtein` | scalar | arity-compatible | **IDENTICAL** — Rename only; both compute insertions+deletions+substitutions (code-point) | string |
| `ln` | scalar | arity-compatible | **IDENTICAL** — NaN on x<=0 in both | numeric |
| `log10` | scalar | arity-compatible; sqlglot→log | **IDENTICAL** — Name-collision trap: DuckDB single-arg log(x) IS log10 (PostgreSQL convention) while Trino log(b,x) is log-base-b — map log10->log10 explicitly, never to bar... | numeric |
| `log2` | scalar | arity-compatible; sqlglot→log | **IDENTICAL** | numeric |
| `lpad` | scalar | arity-compatible | **IDENTICAL** — Probe-verified aligned: pads to code-point count including emoji and CJK (empty-pad edge from mapping doc not covered by this probe) | string, unicode |
| `map` | scalar | arity-compatible; sqlglot→array_map | **IDENTICAL** | string |
| `map_keys` | scalar | arity-compatible | **IDENTICAL** | string |
| `map_values` | scalar | arity-compatible | **IDENTICAL** | string |
| `max` | aggregate | arity-unknown | **IDENTICAL** — Including top/bottom-n variant | aggregate |
| `min` | aggregate | arity-unknown | **IDENTICAL** — Including top/bottom-n variant | aggregate |
| `minute` | scalar | arity-compatible | **IDENTICAL** — Type-gated to DATE / TIMESTAMP-no-TZ in their pushdown | datetime |
| `month` | scalar | arity-compatible | **IDENTICAL** — Aligned on DATE and TIMESTAMP input | datetime |
| `nullif` | macro | arity-unknown | **IDENTICAL** | null |
| `pi` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `pow` | scalar | arity-unknown; sqlglot→power | **IDENTICAL** | numeric |
| `power` | scalar | arity-unknown | **IDENTICAL** | numeric |
| `quarter` | scalar | arity-compatible | **IDENTICAL** | datetime |
| `radians` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `regexp_extract` | scalar | arity-compatible | **IDENTICAL** — Probe-verified aligned: group 0 = whole match in both | regex, unicode |
| `regr_intercept` | aggregate | arity-compatible | **IDENTICAL** | aggregate, numeric |
| `regr_slope` | aggregate | arity-compatible | **IDENTICAL** | aggregate, numeric |
| `replace` | scalar | arity-compatible | **IDENTICAL** — Probe-verified aligned code-point-level; composed vs decomposed inputs treated as distinct on both sides | string, unicode |
| `rpad` | scalar | arity-compatible | **IDENTICAL** — Probe-verified aligned (same caveat as lpad on empty pad string) | string, unicode |
| `second` | scalar | arity-compatible | **IDENTICAL** — Type-gated to DATE / TIMESTAMP-no-TZ in their pushdown | datetime |
| `sign` | scalar | arity-compatible | **IDENTICAL** — Float NaN behavior verified to align (both NaN->NaN) | numeric |
| `sinh` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `skewness` | aggregate | arity-compatible | **IDENTICAL** | aggregate, numeric |
| `sqrt` | scalar | arity-compatible | **IDENTICAL** — NaN on negative input in both | numeric |
| `starts_with` | scalar | arity-unknown | **IDENTICAL** — Probe-verified aligned (binary prefix on UTF-8 is codepoint-aligned) | string, unicode |
| `stddev_pop` | aggregate | arity-compatible | **IDENTICAL** — Trino bare variance/stddev are aliases for the _samp forms | aggregate, numeric |
| `stddev_samp` | aggregate | arity-compatible | **IDENTICAL** — Trino bare variance/stddev are aliases for the _samp forms | aggregate, numeric |
| `sum` | aggregate | arity-compatible | **IDENTICAL** — Both skip NULLs; all-NULL -> NULL; both throw on overflow | aggregate, null, numeric |
| `tanh` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `to_base64` | scalar | arity-compatible | **IDENTICAL** — Standard alphabet in both | string |
| `translate` | scalar | arity-compatible | **IDENTICAL** — Probe-verified aligned: code-point-wise substitution; extra from-chars deleted in both | string, unicode |
| `unhex` | scalar | arity-compatible | **IDENTICAL** — Rename only; returns BLOB | string |
| `url_decode` | scalar | arity-compatible | **IDENTICAL** — Probe sanity check: all NULL-propagate correctly | string, null |
| `url_encode` | scalar | arity-compatible | **IDENTICAL** — Probe sanity check: all NULL-propagate correctly | string, null |
| `var_pop` | aggregate | arity-compatible; sqlglot→variance_pop | **IDENTICAL** — Trino bare variance/stddev are aliases for the _samp forms | aggregate, numeric |
| `var_samp` | aggregate | arity-compatible; sqlglot→variance | **IDENTICAL** — Trino bare variance/stddev are aliases for the _samp forms | aggregate, numeric |
| `week` | scalar | arity-compatible | **IDENTICAL** — Both ISO-8601 (Mon-start, week 01 contains Jan 4); probed: week('2023-01-01')=52, week('2024-12-30')=1 | datetime |
| `acos` | scalar | arity-compatible | — | baseline |
| `acosh` | scalar | arity-compatible | — | baseline |
| `array_append` | macro | arity-unknown | — | baseline |
| `array_cross_product` | scalar | arity-compatible | — | baseline |
| `asin` | scalar | arity-compatible | — | baseline |
| `asinh` | scalar | arity-compatible | — | baseline |
| `atan` | scalar | arity-compatible | — | baseline |
| `atan2` | scalar | arity-compatible | — | baseline |
| `atanh` | scalar | arity-compatible | — | baseline |
| `bin` | scalar | arity-compatible | — | baseline |
| `century` | scalar | arity-compatible | — | baseline |
| `cos` | scalar | arity-compatible | — | baseline |
| `cot` | scalar | arity-compatible | — | baseline |
| `current_catalog` | macro | arity-compatible | — | baseline |
| `current_database` | scalar | arity-compatible | — | baseline |
| `current_user` | macro | arity-compatible | — | baseline |
| `date_add` | macro | arity-compatible | — | baseline |
| `date_sub` | scalar | arity-suspect | — | baseline |
| `datediff` | scalar | arity-suspect | — | baseline |
| `dayname` | scalar | arity-compatible | — | baseline |
| `dayofmonth` | scalar | arity-compatible | — | baseline |
| `dayofweek` | scalar | arity-compatible | — | baseline |
| `dense_rank` | aggregate | arity-unknown | — | baseline |
| `even` | scalar | arity-compatible | — | baseline |
| `factorial` | scalar | arity-unknown | — | baseline |
| `first_value` | aggregate | arity-compatible | — | baseline |
| `instr` | scalar | arity-compatible; sqlglot→locate | — | baseline |
| `json_array` | scalar | arity-unknown | — | baseline |
| `json_contains` | scalar | arity-compatible | — | baseline |
| `json_each` | table | arity-compatible | — | baseline |
| `json_keys` | scalar | arity-compatible | — | baseline |
| `json_object` | scalar | arity-unknown | — | baseline |
| `json_quote` | scalar | arity-compatible | — | baseline |
| `json_type` | scalar | arity-compatible | — | baseline |
| `json_valid` | scalar | arity-compatible | — | baseline |
| `lag` | aggregate | arity-unknown | — | baseline |
| `last_value` | aggregate | arity-compatible | — | baseline |
| `lead` | aggregate | arity-unknown | — | baseline |
| `left` | scalar | arity-compatible | — | baseline |
| `log` | scalar | arity-compatible | — | baseline |
| `map_contains_entry` | macro | arity-unknown | — | baseline |
| `map_contains_value` | macro | arity-unknown | — | baseline |
| `md5` | scalar | arity-compatible | — | baseline |
| `median` | aggregate | arity-compatible; sqlglot→percentile_cont | — | baseline |
| `microsecond` | scalar | arity-compatible | — | baseline |
| `monthname` | scalar | arity-compatible | — | baseline |
| `nth_value` | aggregate | arity-compatible | — | baseline |
| `ntile` | aggregate | arity-compatible | — | baseline |
| `parquet_bloom_probe` | table | arity-unknown | — | baseline |
| `parquet_file_metadata` | table | arity-unknown | — | baseline |
| `parquet_kv_metadata` | table | arity-unknown | — | baseline |
| `percent_rank` | aggregate | arity-unknown | — | baseline |
| `query` | table | arity-unknown | — | baseline |
| `rank` | aggregate | arity-unknown | — | baseline |
| `regr_avgx` | aggregate | arity-compatible | — | baseline |
| `regr_avgy` | aggregate | arity-compatible | — | baseline |
| `regr_count` | aggregate | arity-compatible | — | baseline |
| `regr_r2` | aggregate | arity-compatible | — | baseline |
| `regr_sxx` | aggregate | arity-compatible | — | baseline |
| `regr_sxy` | aggregate | arity-compatible | — | baseline |
| `regr_syy` | aggregate | arity-compatible | — | baseline |
| `repeat` | scalar | arity-compatible | — | baseline |
| `right` | scalar | arity-compatible | — | baseline |
| `row_number` | aggregate | arity-unknown | — | baseline |
| `sem` | aggregate | arity-compatible | — | baseline |
| `session_user` | macro | arity-compatible | — | baseline |
| `sha1` | scalar | arity-compatible; sqlglot→sha | — | baseline |
| `signbit` | scalar | arity-compatible | — | baseline |
| `sin` | scalar | arity-compatible | — | baseline |
| `st_astext` | scalar | arity-compatible | — | baseline |
| `st_geomfromwkb` | scalar | arity-compatible | — | baseline |
| `tan` | scalar | arity-compatible | — | baseline |
| `to_days` | scalar | arity-compatible | — | baseline |
| `to_json` | scalar | arity-compatible | — | baseline |
| `to_seconds` | scalar | arity-compatible | — | baseline |
| `unnest` | table | arity-unknown; sqlglot→explode | — | baseline |
| `user` | macro | arity-compatible | — | baseline |
| `version` | scalar | arity-compatible | — | baseline |
| `weekday` | scalar | arity-compatible | — | baseline |
| `weekofyear` | scalar | arity-compatible | — | baseline |
| `xor` | scalar | arity-compatible | — | baseline |
| `yearweek` | scalar | arity-compatible | — | baseline |

## trino → doris

Bucket A same-name candidates: **158**. With prior trino↔duckdb evidence: **148** (of which **21** prior-DIVERGENT).

| function | kinds | arity note | prior trino↔duckdb verdict + hazard | suggested probe areas |
| --- | --- | --- | --- | --- |
| `array_join` | scalar | arity-compatible | **DIVERGENT** — NULL elements: Trino's third arg substitutes a replacement; DuckDB silently drops NULLs | null, string |
| `array_max` | scalar | arity-compatible | **DIVERGENT** — Trino propagates NULL if ANY element is NULL; DuckDB skips NULLs — do not map without explicit NULL-free guarantee | null |
| `array_min` | scalar | arity-compatible | **DIVERGENT** — Trino propagates NULL if ANY element is NULL; DuckDB skips NULLs — do not map without explicit NULL-free guarantee | null |
| `bit_count` | scalar | arity-suspect | **DIVERGENT** — Trino requires explicit bit-width argument; DuckDB infers from type — do not translate without matching width | numeric |
| `concat` | scalar | arity-compatible | **DIVERGENT** — Probe-verified on every NULL-bearing case: concat('a',NULL,'c','d')='acd', concat(NULL,'b','c')='bc', concat(NULL,NULL)='' in DuckDB; Trino returns NULL in a... | string, null |
| `date_add` | scalar | arity-suspect | **DIVERGENT** — Same name, incompatible signatures: DuckDB date_add(date, interval) vs Trino date_add(unit, value, x) — translate to interval arithmetic, never by name | datetime |
| `date_format` | scalar | arity-compatible | **DIVERGENT** — Tier D 'never push': Joda-Time format strings (yyyy-MM-dd) vs C strftime (%Y-%m-%d) are incompatible; safe only if a translation layer owns the format-string... | datetime, string |
| `greatest` | scalar | arity-unknown | **DIVERGENT** — NULL handling differs: Trino returns NULL if ANY arg is NULL; DuckDB SKIPS NULLs — never translate by name when args may be NULL | null |
| `least` | scalar | arity-unknown | **DIVERGENT** — NULL handling differs: Trino returns NULL if ANY arg is NULL; DuckDB SKIPS NULLs — never translate by name when args may be NULL | null |
| `lower` | scalar | arity-compatible | **DIVERGENT** — Probe-verified: Turkish 'İ' -> DuckDB 'i' vs Trino 'i'+U+0307 (full case folding); ASCII and most non-ASCII aligned. Needed native ICU extension for parity | string, unicode |
| `ltrim` | scalar | arity-compatible | **DIVERGENT** — Same whitespace-set divergence as trim (tab/LF/CR/FF/VT not stripped by DuckDB default) | string, unicode |
| `max_by` | aggregate | arity-compatible | **DIVERGENT** — Different names AND NULL semantics: Trino skips NULLs; DuckDB has separate arg_min_null/arg_max_null variants — 'do not push by name' | aggregate, null |
| `min_by` | aggregate | arity-compatible | **DIVERGENT** — Different names AND NULL semantics: Trino skips NULLs; DuckDB has separate arg_min_null/arg_max_null variants — 'do not push by name' | aggregate, null |
| `regexp_extract_all` | scalar | arity-compatible | **DIVERGENT** — 'Empty-match handling differs; verify before pushing' | regex |
| `regexp_replace` | scalar | arity-compatible | **DIVERGENT** — Trino replaces ALL matches by default; DuckDB replaces first match unless 'g' flag — parity macro forces 'g' | regex |
| `reverse` | scalar | arity-compatible | **DIVERGENT** — Probe-verified: DuckDB reverse is GRAPHEME-cluster-aware, Trino code-point-only — decomposed 'café' reverses keeping e+U+0301 together in DuckDB; ZWJ family ... | string, unicode |
| `rtrim` | scalar | arity-compatible | **DIVERGENT** — Same whitespace-set divergence as trim | string, unicode |
| `split` | scalar | arity-compatible; sqlglot→split_by_string | **DIVERGENT** — Empty-string delimiter: Trino splits to character array, DuckDB returns single-element array; Trino limit form has no equivalent | string |
| `split_part` | scalar | arity-compatible | **DIVERGENT** — Out-of-bounds index: Trino returns NULL, DuckDB returns empty string — do not translate by name without NULLIF wrap | string, null |
| `trim` | scalar | arity-compatible | **DIVERGENT** — Probe-verified: DuckDB bare trim strips space + EM SPACE but NOT tab/LF/CR/FF/VT; Trino (Java Character.isWhitespace) strips all of those; both leave NBSP/ZW... | string, unicode |
| `upper` | scalar | arity-compatible | **DIVERGENT** — Probe-verified: German 'ß' -> DuckDB 'ẞ' (U+1E9E) vs Trino 'SS' (Java full folding, length-changing); ASCII aligned. Needed native ICU extension | string, unicode |
| `round` | scalar | arity-compatible | **UNCLEAR** — 'Trino: round is half-up. DuckDB: round is half-away-from-zero (since 0.10) — verify per version... Do NOT push when d > 0 until verified.' DuckDB also has r... | numeric |
| `any_value` | aggregate | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Order-dependence undefined in both unless ORDER BY used | aggregate |
| `array_agg` | aggregate | arity-compatible; sqlglot→collect_list | **CONDITIONALLY_EQUIVALENT** — DuckDB returns LIST vs Trino ARRAY — type-layer difference | aggregate |
| `array_sort` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — NULL ordering: both default NULLS LAST but flagged 'verify if your data has NULLs'; comparator (lambda) form has a different shape and is not mappable | null |
| `cosine_distance` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Trino has sparse (map) and dense array forms; DuckDB splits array vs list — verify shape before mapping | numeric |
| `cosine_similarity` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Trino has sparse (map) and dense array forms; DuckDB splits array vs list — verify shape before mapping | numeric |
| `current_date` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Both session-local; do not push if behavior must match a specific timezone definition | datetime, timezone |
| `date_trunc` | scalar | arity-unknown | **CONDITIONALLY_EQUIVALENT** — Aligned for intersection unit set (second..year) but DuckDB always returns TIMESTAMP even for DATE input while Trino preserves input type; numeric comparison... | datetime |
| `dow` | scalar | arity-compatible; sqlglot→dayofweek_iso | **CONDITIONALLY_EQUIVALENT** — Trino day_of_week is 1..7 Monday=1; DuckDB dayofweek/date_part('dow') is 0..6 SUNDAY=0 — must map to isodow, never dayofweek. Pinned: '2024-01-07' -> 7 | datetime |
| `from_unixtime` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Trino from_unixtime(double) returns TIMESTAMP(3) WITH TIME ZONE and uses session TZ — they reclassified it Tier C (session-zone-dependent), not a plain rename | datetime, timezone |
| `hour` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Probe-verified equivalent ONLY with SET TimeZone = Trino session zone before evaluation; without it, year(tz_col)=2024 can silently return 2025 rows (Asia/Si... | datetime, timezone |
| `json_extract` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Path syntax differs (Trino restricted JSONPath subset vs DuckDB JSONPath-ish with wildcards); safe only for simple $.a.b property paths; DuckDB side lives in... | string, extension |
| `kurtosis` | aggregate | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Trino kurtosis = sample excess; DuckDB has both sample and pop variants — 'push only with explicit match' | aggregate, numeric |
| `map_entries` | scalar | arity-unknown | **CONDITIONALLY_EQUIVALENT** — Element type differs: Trino row(K,V) vs DuckDB struct(key,value); needs type-layer mapping | string |
| `md5` | scalar | arity-compatible; sqlglot→md5_digest | **CONDITIONALLY_EQUIVALENT** — Probe-verified: DuckDB md5 NULL-propagates correctly (md5(NULL)->NULL) and hex digest matches; but returns hex VARCHAR vs Trino VARBINARY — needs unhex() wrap | hash, null |
| `now` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Both bind once per query, but default precision differs: Trino timestamp(3) vs DuckDB microseconds(6) — cast carefully when comparing | datetime |
| `rand` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Non-deterministic (never pushable); Trino bounded forms random(n), random(m,n) have no DuckDB equivalent | numeric |
| `random` | scalar | arity-compatible; sqlglot→rand | **CONDITIONALLY_EQUIVALENT** — Non-deterministic (never pushable); Trino bounded forms random(n), random(m,n) have no DuckDB equivalent | numeric |
| `replace` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Trino 2-arg remove form has no DuckDB arity; map by passing '' as replacement | string |
| `sequence` | scalar | arity-compatible; sqlglot→generate_series | **CONDITIONALLY_EQUIVALENT** — Trino sequence is INCLUSIVE on stop; DuckDB generate_series inclusive but range() is EXCLUSIVE — choose by inclusivity | numeric |
| `sha1` | scalar | arity-compatible; sqlglot→sha | **CONDITIONALLY_EQUIVALENT** — Same pattern as md5: NULL propagation verified aligned; unhex wrap required for VARBINARY return | hash, null |
| `soundex` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Only available when splink_udfs community extension is loaded | string, extension |
| `substring` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Verified aligned on code-point unit (both split combining marks/ZWJ/flags at codepoint boundaries); negative start counts from end in both; but start=0 unver... | string, unicode |
| `uuid` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Non-deterministic — same distribution but never pushable/comparable | numeric |
| `year` | scalar | arity-compatible | **CONDITIONALLY_EQUIVALENT** — Probe-verified equivalent ONLY with SET TimeZone = Trino session zone before evaluation; without it, year(tz_col)=2024 can silently return 2025 rows (Asia/Si... | datetime, timezone |
| `array_except` | scalar | arity-compatible | **NO_EQUIVALENT** | string |
| `array_remove` | scalar | arity-unknown | **NO_EQUIVALENT** | string |
| `array_union` | scalar | arity-compatible | **NO_EQUIVALENT** — No direct DuckDB form | string |
| `crc32` | scalar | arity-compatible | **NO_EQUIVALENT** — No extension cover either | hash |
| `e` | scalar | arity-compatible | **NO_EQUIVALENT** — DuckDB has no e(); use exp(1) | numeric |
| `json_parse` | scalar | arity-compatible | **NO_EQUIVALENT** — Different API (DuckDB uses casts / to_json / from_json) | string |
| `map_agg` | aggregate | arity-compatible | **NO_EQUIVALENT** | aggregate |
| `normal_cdf` | scalar | arity-compatible | **NO_EQUIVALENT** | numeric |
| `regexp_count` | scalar | arity-compatible | **NO_EQUIVALENT** — No direct DuckDB form | regex |
| `shuffle` | scalar | arity-compatible | **NO_EQUIVALENT** — shuffle also non-deterministic | string |
| `to_iso8601` | scalar | arity-compatible | **NO_EQUIVALENT** — No direct function; push as cast or do not push | datetime, string |
| `truncate` | scalar | arity-compatible | **NO_EQUIVALENT** — DuckDB trunc is 1-arg only; would need cast(x*pow(10,n))/pow(10,n) shim — deferred | numeric |
| `width_bucket` | scalar | arity-compatible | **NO_EQUIVALENT** — Different shape; not 1:1 | numeric |
| `abs` | scalar | arity-compatible | **IDENTICAL** — abs(MIN_INT): both throw | numeric |
| `acos` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `array_distinct` | scalar | arity-compatible | **IDENTICAL** — Name trap: DuckDB array_unique returns the COUNT, not the distinct list — must map to list_distinct | string |
| `array_first` | scalar | arity-compatible | **IDENTICAL** | string |
| `array_intersect` | scalar | arity-compatible | **IDENTICAL** | string |
| `array_last` | scalar | arity-suspect | **IDENTICAL** | string |
| `array_position` | scalar | arity-unknown | **IDENTICAL** — Both 1-based, 0 if not found | string |
| `asin` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `atan` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `atan2` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `avg` | aggregate | arity-compatible | **IDENTICAL** — Skips NULLs in both | aggregate, null |
| `bool_and` | aggregate | arity-compatible; sqlglot→min | **IDENTICAL** | aggregate |
| `bool_or` | aggregate | arity-compatible; sqlglot→max | **IDENTICAL** | aggregate |
| `cbrt` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `ceil` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `ceiling` | scalar | arity-compatible; sqlglot→ceil | **IDENTICAL** | numeric |
| `concat_ws` | scalar | arity-compatible | **IDENTICAL** — Probe-verified aligned on ALL NULL shapes: NULL elements skipped by both, NULL separator -> NULL in both, all-NULL elements -> '' in both; Unicode separator OK | string, unicode, null |
| `corr` | aggregate | arity-compatible | **IDENTICAL** | aggregate, numeric |
| `cos` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `cosh` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `count` | aggregate | arity-compatible | **IDENTICAL** — Both skip NULLs for count(expr) | aggregate, null |
| `covar_pop` | aggregate | arity-compatible | **IDENTICAL** | aggregate, numeric |
| `covar_samp` | aggregate | arity-compatible | **IDENTICAL** | aggregate, numeric |
| `cume_dist` | window | arity-unknown | **IDENTICAL** — Trino 481 docs claim '-> bigint' but it actually returns double like DuckDB — doc bug, trust the type system | aggregate, numeric |
| `day` | scalar | arity-compatible | **IDENTICAL** — Aligned on DATE and TIMESTAMP input | datetime |
| `degrees` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `dense_rank` | window | arity-unknown | **IDENTICAL** | aggregate |
| `doy` | scalar | arity-compatible; sqlglot→dayofyear | **IDENTICAL** — Rename; pinned by leap-day fixture '2024-02-29' -> 60 | datetime |
| `exp` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `first_value` | window | arity-compatible | **IDENTICAL** — DuckDB additionally supports IGNORE NULLS grammar | aggregate, null |
| `floor` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `from_base64` | scalar | arity-compatible | **IDENTICAL** | string |
| `from_hex` | scalar | arity-compatible; sqlglot→unhex | **IDENTICAL** — Rename only; returns BLOB | string |
| `hamming_distance` | scalar | arity-compatible | **IDENTICAL** — Rename only; both raise on unequal-length input | string |
| `histogram` | aggregate | arity-compatible | **IDENTICAL** — Counts by value in both | aggregate |
| `lag` | window | arity-unknown | **IDENTICAL** — DuckDB additionally supports IGNORE NULLS grammar | aggregate, null |
| `last_value` | window | arity-compatible | **IDENTICAL** — DuckDB additionally supports IGNORE NULLS grammar | aggregate, null |
| `lead` | window | arity-unknown | **IDENTICAL** — DuckDB additionally supports IGNORE NULLS grammar | aggregate, null |
| `length` | scalar | arity-compatible; sqlglot→char_length | **IDENTICAL** — Probe-verified aligned on full Unicode corpus: code points, emoji counted as 1 (4-byte U+1F600 = length 1) | string, unicode |
| `levenshtein_distance` | scalar | arity-compatible; sqlglot→levenshtein | **IDENTICAL** — Rename only; both compute insertions+deletions+substitutions (code-point) | string |
| `ln` | scalar | arity-compatible | **IDENTICAL** — NaN on x<=0 in both | numeric |
| `log10` | scalar | arity-compatible; sqlglot→log | **IDENTICAL** — Name-collision trap: DuckDB single-arg log(x) IS log10 (PostgreSQL convention) while Trino log(b,x) is log-base-b — map log10->log10 explicitly, never to bar... | numeric |
| `log2` | scalar | arity-compatible; sqlglot→log | **IDENTICAL** | numeric |
| `lpad` | scalar | arity-compatible | **IDENTICAL** — Probe-verified aligned: pads to code-point count including emoji and CJK (empty-pad edge from mapping doc not covered by this probe) | string, unicode |
| `map` | scalar | arity-compatible; sqlglot→array_map | **IDENTICAL** | string |
| `map_keys` | scalar | arity-compatible | **IDENTICAL** | string |
| `map_values` | scalar | arity-compatible | **IDENTICAL** | string |
| `max` | aggregate | arity-unknown | **IDENTICAL** — Including top/bottom-n variant | aggregate |
| `min` | aggregate | arity-unknown | **IDENTICAL** — Including top/bottom-n variant | aggregate |
| `minute` | scalar | arity-compatible | **IDENTICAL** — Type-gated to DATE / TIMESTAMP-no-TZ in their pushdown | datetime |
| `month` | scalar | arity-compatible | **IDENTICAL** — Aligned on DATE and TIMESTAMP input | datetime |
| `nth_value` | window | arity-compatible | **IDENTICAL** — DuckDB additionally supports IGNORE NULLS grammar | aggregate, null |
| `ntile` | window | arity-compatible | **IDENTICAL** | aggregate |
| `percent_rank` | window | arity-unknown | **IDENTICAL** | aggregate |
| `pi` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `pow` | scalar | arity-compatible; sqlglot→power | **IDENTICAL** | numeric |
| `power` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `quarter` | scalar | arity-compatible | **IDENTICAL** | datetime |
| `radians` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `rank` | window | arity-unknown | **IDENTICAL** | aggregate |
| `regexp_extract` | scalar | arity-compatible | **IDENTICAL** — Probe-verified aligned: group 0 = whole match in both | regex, unicode |
| `regr_intercept` | aggregate | arity-compatible | **IDENTICAL** | aggregate, numeric |
| `regr_slope` | aggregate | arity-compatible | **IDENTICAL** | aggregate, numeric |
| `row_number` | window | arity-unknown | **IDENTICAL** | aggregate |
| `rpad` | scalar | arity-compatible | **IDENTICAL** — Probe-verified aligned (same caveat as lpad on empty pad string) | string, unicode |
| `second` | scalar | arity-compatible | **IDENTICAL** — Type-gated to DATE / TIMESTAMP-no-TZ in their pushdown | datetime |
| `sign` | scalar | arity-compatible | **IDENTICAL** — Float NaN behavior verified to align (both NaN->NaN) | numeric |
| `sin` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `sinh` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `skewness` | aggregate | arity-compatible | **IDENTICAL** | aggregate, numeric |
| `sqrt` | scalar | arity-compatible | **IDENTICAL** — NaN on negative input in both | numeric |
| `starts_with` | scalar | arity-compatible | **IDENTICAL** — Probe-verified aligned (binary prefix on UTF-8 is codepoint-aligned) | string, unicode |
| `stddev` | aggregate | arity-compatible | **IDENTICAL** — Trino bare variance/stddev are aliases for the _samp forms | aggregate, numeric |
| `stddev_pop` | aggregate | arity-compatible | **IDENTICAL** — Trino bare variance/stddev are aliases for the _samp forms | aggregate, numeric |
| `stddev_samp` | aggregate | arity-compatible | **IDENTICAL** — Trino bare variance/stddev are aliases for the _samp forms | aggregate, numeric |
| `sum` | aggregate | arity-compatible | **IDENTICAL** — Both skip NULLs; all-NULL -> NULL; both throw on overflow | aggregate, null, numeric |
| `tan` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `tanh` | scalar | arity-compatible | **IDENTICAL** | numeric |
| `to_base64` | scalar | arity-compatible | **IDENTICAL** — Standard alphabet in both | string |
| `to_hex` | scalar | arity-compatible; sqlglot→hex | **IDENTICAL** — Rename only | string |
| `translate` | scalar | arity-compatible | **IDENTICAL** — Probe-verified aligned: code-point-wise substitution; extra from-chars deleted in both | string, unicode |
| `url_decode` | scalar | arity-compatible | **IDENTICAL** — Probe sanity check: all NULL-propagate correctly | string, null |
| `url_encode` | scalar | arity-compatible | **IDENTICAL** — Probe sanity check: all NULL-propagate correctly | string, null |
| `var_pop` | aggregate | arity-compatible; sqlglot→variance_pop | **IDENTICAL** — Trino bare variance/stddev are aliases for the _samp forms | aggregate, numeric |
| `var_samp` | aggregate | arity-compatible; sqlglot→variance | **IDENTICAL** — Trino bare variance/stddev are aliases for the _samp forms | aggregate, numeric |
| `variance` | aggregate | arity-compatible | **IDENTICAL** — Trino bare variance/stddev are aliases for the _samp forms | aggregate, numeric |
| `week` | scalar | arity-compatible; sqlglot→weekofyear | **IDENTICAL** — Both ISO-8601 (Mon-start, week 01 contains Jan 4); probed: week('2023-01-01')=52, week('2024-12-30')=1 | datetime |
| `year_of_week` | scalar | arity-compatible | **IDENTICAL** — DuckDB has no bare isoyear() function — reach via extract; cast to BIGINT; pinned '2024-12-30' -> 2025 | datetime |
| `yow` | scalar | arity-compatible | **IDENTICAL** — DuckDB has no bare isoyear() function — reach via extract; cast to BIGINT; pinned '2024-12-30' -> 2025 | datetime |
| `arrays_overlap` | scalar | arity-compatible | — | baseline |
| `cardinality` | scalar | arity-compatible; sqlglot→array_length | — | baseline |
| `date` | scalar | arity-compatible | — | baseline |
| `element_at` | scalar | arity-compatible | — | baseline |
| `format_number` | scalar | arity-compatible | — | baseline |
| `from_iso8601_date` | scalar | arity-compatible; sqlglot→cast | — | baseline |
| `log` | scalar | arity-compatible | — | baseline |
| `parse_data_size` | scalar | arity-compatible | — | baseline |
| `repeat` | scalar | arity-compatible | — | baseline |
| `substr` | scalar | arity-compatible; sqlglot→substring | — | baseline |
